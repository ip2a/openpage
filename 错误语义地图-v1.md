# 错误语义地图 v1

> 更新时间：2026-06-05  
> 范围：OpenPage 当前 **TCP daemon CLI** 的 shell/control-plane 错误语义

---

## 1. 目的

这份文档只回答一个问题：

> **当前 CLI JSON shell 里，不同错误为什么会落到不同的 `error.kind`？**

目标不是解释所有运行时细节，而是给后续维护提供一个稳定的判断基线，避免：

- 同类输入错误落到两种机器语义
- runtime/stateful 错误被误归类成 `invalid_input`
- 后续修改把已经收紧过的 contract 再放松

当前真相源以代码为准：

- `rust/src/cli/protocol.rs`
  - `openpage_error_kind(...)`
  - `detail_maps_to_invalid_input(...)`
  - `simple_openpage_error(...)`
  - `response_openpage_error(...)`
- `rust/src/cli/oneshot.rs`
  - `response_result(...)`

---

## 2. 当前主要 `error.kind`

| `error.kind` | 语义 | 典型来源 | 调用方应该怎么理解 |
|---|---|---|---|
| `invalid_input` | 输入本身不合法 | 顶层 parse error、batch malformed input、direct invalid-value check、daemon-side 参数/范围/空值校验 | **改输入再试** |
| `unsupported_operation` | 功能/平台/工作流限制 | `batch cannot execute serve`、`downloads open is unsupported on this platform` | **当前语义下不支持**，不是简单重试 |
| `browser_operation` | 浏览器/页面/运行时语义错误 | inactive session、unknown target、真实页面状态问题 | **输入可能没错，但当前运行时状态不满足** |
| `daemon_transient` | daemon 短暂性故障 | 连接重置、短暂不可用、建议重试同命令 | **可以重试** |
| `invalid_json` | 原始 TCP daemon 收到非法 JSON | 原始协议层，不是高层 CLI 命令壳 | **请求载荷坏了** |
| `tcp_error` | TCP 传输级错误 | daemon 连接/读写层 | **传输故障** |
| `io` / `timeout` / `serialization` | 通用底层失败 | 文件、超时、序列化等 | 需要结合 message 看 |

---

## 3. `invalid_input` 的当前边界

当前已经明确收口到 `invalid_input` 的错误，大致分四类。

### 3.1 顶层 CLI 解析失败

例如：

- `openpage page url`
- `openpage serve --stdio`

这类错误来自 clap 解析层，当前统一返回：

- `error.kind="invalid_input"`

并且对于**已移除但有明确迁移路径**的入口，当前还会额外带：

- `error.fix`

当前已覆盖的迁移提示包括：

- `page ...` → 指向当前 top-level TCP CLI 等价命令
- `serve --stdio` → 指向 `openpage serve --session <name>`

---

### 3.2 batch 输入本身坏了

例如：

- `openpage batch "page url"`
- `printf 'not-json' | openpage batch`

这类错误当前也统一返回：

- `error.kind="invalid_input"`

如果 nested parse failure 命中的正好是已移除旧入口，例如：

- `openpage batch "page url"`
- `openpage batch "serve --stdio"`

当前也会继续补同样的：

- `error.fix`

但注意：

- `batch cannot execute serve`
- `batch cannot execute doctor`

这种是 **语义限制**，不是输入格式坏，因此仍然保留：

- `error.kind="unsupported_operation"`

对于当前已知、且有明确 top-level 替代工作流的 batch restriction，payload 现在还会带：

- `error.fix`

当前已覆盖：

- `batch cannot execute serve`
- `batch cannot execute doctor`
- `batch cannot execute nested batch commands`
- `no recently closed tab recorded for this session`

---

### 3.3 direct invalid-value checks

例如：

- `zoom in --step 0`
- `drag-in` 缺少 `--text` / `--files`

这类错误当前返回：

- `error.kind="invalid_input"`

对于 `drag-in` 缺少 payload 这种已知场景，当前还会额外带：

- `error.fix`

同样已经补到结构化 `fix` 的还有一组“枚举值选择错误”：

- `select requires one of: text, value, index`
- `unsupported snapshot mode: ...`
- `unsupported snapshot format: ...`

因为本质是参数值不满足约束，不是 runtime 故障。

---

### 3.4 daemon-side 参数/范围/空值校验

这是前几轮刚收紧的一批，也是最容易漂的地方。

当前已明确进入 `invalid_input` 的典型 detail 包括：

- `history index must be >= 1`
- `history index out of range: ...`
- `find-in-page text must not be empty`
- `unknown navigation token: ...`
- `navigation token ... belongs to another page or frame`
- `missing target`
- `missing string param: ...`
- `missing number param: ...`
- `missing array param: ...`
- `missing param: ...`
- `missing headers param: ...`
- `... must be a string or string array`
- `... must be an integer or integer array`
- `... must be an object`
- `array param must contain only strings: ...`
- `array param must contain only integers: ...`
- `header values must be strings: ...`
- `drag-in requires text or files`

这些错误的共同点是：

> **用户给的 token / param / range / shape 本身就不成立。**

这一组里，当前已经开始给出结构化 `error.fix` 的子类包括：

- `missing string param: ...`
- `missing number param: ...`
- `missing array param: ...`
- `missing headers param: ...`
- `... must be an object`
- `... must be a string or string array`
- `... must be an integer or integer array`

---

## 4. 明确不该落入 `invalid_input` 的类型

下面这些虽然看起来也像“失败”，但不应归到 `invalid_input`：

| 场景 | 现在的 kind | 原因 |
|---|---|---|
| `unknown target: ...` | `browser_operation` | target 来自运行时状态，不只是用户输入拼错 |
| `session ... is not active` | `browser_operation` | 控制面 state 错误 |
| `session ... daemon version mismatch` | `browser_operation` | 控制面 state 错误 |
| `downloads open is unsupported on this platform` | `unsupported_operation` | 平台限制 |
| `batch cannot execute serve` | `unsupported_operation` | workflow restriction |
| `no recently closed tab recorded for this session` | `unsupported_operation` | 当前 session 语义不满足，不是简单输入非法 |

这里的原则是：

> **如果修复方式主要是“换环境 / 换状态 / 先做前置动作”，就不该归到 `invalid_input`。**

---

## 5. 当前最小判断规则

可以用这个简单判断：

### 落到 `invalid_input`

如果错误本质是：

- 命令解析失败
- 参数缺失
- 参数类型不对
- 参数值越界
- token 无效
- 文本为空
- request shape 不合法

### 落到 `unsupported_operation`

如果错误本质是：

- 当前工作流不允许
- 当前平台不支持
- 当前功能面就是故意不支持

### 落到 `browser_operation`

如果错误本质是：

- session / target / page / browser 当前状态不满足
- 输入可能是合理的，但运行时上下文不对

---

## 6. 当前已验证的真实例子

这些都已经做过真实 CLI 验证：

| 命令 | 当前结果 |
|---|---|
| `openpage page url` | `error.kind="invalid_input"` |
| `openpage serve --stdio` | `error.kind="invalid_input"` |
| `openpage batch "page url"` | `error.kind="invalid_input"` |
| `printf 'not-json' | openpage batch` | `error.kind="invalid_input"` |
| `openpage zoom in --step 0 --session doc-agent` | `error.kind="invalid_input"` |
| `openpage history go 0 --session doc-agent` | `error.kind="invalid_input"` |
| `openpage history go 999999 --session doc-agent` | `error.kind="invalid_input"` |
| `openpage find-in-page "" --session doc-agent` | `error.kind="invalid_input"` |
| `openpage wait-for-navigation --token definitely-bad --timeout 1 --session doc-agent` | `error.kind="invalid_input"` |

---

## 7. 当前契约测试

当前这条线已经不再只是“修过几个点”，而是有 contract test 保护。

重点测试：

- `invalid_input_contract_covers_known_detail_taxonomy`
- `invalid_input_contract_keeps_runtime_and_restriction_cases_outside_bucket`

此外还包括多组单点 case，例如：

- `simple_openpage_error_maps_unknown_navigation_token_to_invalid_input`
- `response_result_preserves_invalid_input_kind_for_unknown_navigation_token`
- `simple_openpage_error_maps_find_in_page_empty_text_to_invalid_input`
- `simple_openpage_error_maps_missing_string_param_to_invalid_input`

这意味着后续如果有人把这些错误重新漂回：

- `browser_operation`
- `unsupported_operation`

测试会直接报出来。

---

## 8. 当前阶段性结论

站在 2026-06-04 这个节点，OpenPage 的 shell/control-plane 错误语义已经形成了比较稳定的边界：

- **输入坏了** → `invalid_input`
- **功能本来就不支持** → `unsupported_operation`
- **运行时状态不对** → `browser_operation`
- **daemon 短暂故障可重试** → `daemon_transient`

这份地图的作用不是让后续一劳永逸不改，而是：

> **以后再改时，要先说明为什么某个错误应该跨桶，而不是默默漂移。**
