  ---
  一句话总结
  
  CLI 本身不碰浏览器，它只是个「翻译官 + 传话筒」。Agent 说的每一句人话（open baidu.com、click @e5），CLI 负责翻译成
  JSON，递给后台的 Rust Daemon；Daemon 操纵 Chrome 干活，结果再原路传回，CLI 负责 formatting 给 Agent 看。

  ---
  完整流程：打开百度 → 搜索 → 截图
  
  假设 Agent 要执行这个任务，它本质上会按这个顺序调用 CLI：

  第 1 步：agent-browser open baidu.com

  Agent 视角：启动浏览器，打开百度。

  CLI 内部发生了什么（对应 main.rs + commands.rs + connection.rs）：

  1. 解析命令：commands.rs:parse_command() 把 open baidu.com 翻译成：
  { "id": "r123456", "action": "navigate", "url": "https://baidu.com" }
  2. 检查 Daemon：connection.rs:ensure_daemon() 发现 default.sock 不存在 → 启动后台 Daemon 进程。
  3. 传递配置：CLI 把 --headed、--proxy 等配置通过环境变量（AGENT_BROWSER_*）注入给 Daemon。
  4. IPC 发送：connection.rs:send_command() 把 JSON 通过 Unix Socket 发给 Daemon。
  5. Daemon 干活：Daemon 通过 CDP 协议启动 Chrome，导航到百度。
  6. 结果返回：Daemon 回传 { "success": true }，output.rs 打印 ✓ 或 JSON。

  ▎ 关键设计：Daemon 启动后会一直活着。后面所有命令都复用这个 session，不用重复开浏览器。

  ---
  第 2 步：agent-browser snapshot -i

  Agent 视角：看看页面上有什么能点的。

  CLI 内部：

  1. commands.rs 翻译为：
  { "id": "r234567", "action": "snapshot", "interactive": true }
  2. 通过已有 Socket 发给 Daemon（这次不用启动新 Daemon）。
  3. Daemon 遍历页面可交互元素（按钮、输入框、链接），生成带 @ref 的文本树。
  4. CLI output.rs 把结果打印给 Agent，大概长这样：
  [1] <input> 搜索框
  [2] <button> 百度一下
  [3] <a> 新闻
  ...
  @e5 [1] <input> 搜索框
  @e12 [2] <button> 百度一下

  ▎ 这是 agent-browser 最核心的 AI 设计：用 @e5 这种紧凑引用代替长 CSS 选择器，给 LLM 省 token。

  ---
  第 3 步：agent-browser fill @e5 "蓝色的电风扇"
  
  Agent 视角：在搜索框输入文字。

  CLI 内部：

  1. commands.rs 看到 fill 命令，翻译成：
  { "id": "r345678", "action": "fill", "selector": "@e5", "value": "蓝色的电风扇" }
  2. Daemon 收到后，把 @e5 解引用回真实 DOM 节点，调用 CDP 的 DOM.setNodeValue。

  ---
  第 4 步：agent-browser click @e12
  
  Agent 视角：点击搜索按钮。

  CLI 内部：

  1. 翻译为：
  { "id": "r456789", "action": "click", "selector": "@e12" }
  2. Daemon 通过 CDP 触发点击，页面跳转进入搜索结果页。

  ---
  第 5 步：agent-browser wait --load networkidle
  
  Agent 视角：等页面加载完。

  CLI 内部：

  1. 翻译为：
  { "id": "r567890", "action": "waitforloadstate", "state": "networkidle" }
  2. Daemon 监听 Chrome 的网络空闲事件，完成后返回。

  ---
  第 6 步：agent-browser snapshot -i（第二次）
  
  Agent 视角：看搜索结果，找第一个链接。

  CLI 内部：同上，返回新的 @ref 列表，Agent 看到第一个结果是 @e8。

  ---
  第 7 步：agent-browser click @e8
  
  Agent 视角：点进第一个商品/结果页。

  CLI 内部：Daemon 执行点击，页面跳转到商品详情页。

  ---
  第 8 步：agent-browser scroll
  
  Agent 视角：往下浏览一下。

  CLI 内部：

  { "id": "r678901", "action": "scroll", "direction": "down", "amount": 300 }

  ---
  第 9 步：agent-browser screenshot
  
  Agent 视角：截图发给我。

  CLI 内部：

  1. 翻译为：
  { "id": "r789012", "action": "screenshot", "path": null, "selector": null, "fullPage": false }
  2. Daemon 通过 CDP 截取当前视口，生成 PNG 二进制。
  3. 如果指定了 path，Daemon 直接存文件；否则返回 base64，CLI output.rs 按格式输出。

  ---
  流程图：CLI 在哪里介入
  
  ┌─────────────┐     ┌──────────────┐     ┌──────────────┐     ┌──────────┐
  │   AI Agent  │────▶│  CLI (Rust)  │────▶│Daemon (Rust) │────▶│ Chrome   │
  │  (LLM/脚本) │     │              │     │              │     │ (CDP)    │
  └─────────────┘     └──────────────┘     └──────────────┘     └──────────┘
                             │                                          │
        ① parse_command()  │                                   执行浏览器操作
        ② ensure_daemon()  │                                          │
        ③ send_command()   │◄───────────────────────────────────────┘
        ④ print_response() │         返回 JSON 结果
                             │
                      output.rs 格式化
                             │
                             ▼
                     ┌──────────────┐
                     │  Human / JSON│
                     │   输出给 Agent │
                     └──────────────┘

  ---
  CLI 各模块在流程中的职责
  
  ┌──────────────────┬───────────────────────────────┬──────────────────────────────────────────────────────┐
  │       阶段       │           代码位置            │                       通俗解释                       │
  ├──────────────────┼───────────────────────────────┼──────────────────────────────────────────────────────┤
  │ 接收命令         │ main.rs:490-666               │ 判断是哪种命令，需不需要 daemon                      │
  ├──────────────────┼───────────────────────────────┼──────────────────────────────────────────────────────┤
  │ 解析成 JSON      │ commands.rs:parse_command()   │ 把 click @e5 翻译成 {action:"click", selector:"@e5"} │
  ├──────────────────┼───────────────────────────────┼──────────────────────────────────────────────────────┤
  │ 加载配置         │ flags.rs                      │ 看看有没有 --headed、代理、session 名                │
  ├──────────────────┼───────────────────────────────┼──────────────────────────────────────────────────────┤
  │ 启动/连接 Daemon │ connection.rs:ensure_daemon() │ 没 daemon 就启动一个，有就复用                       │
  ├──────────────────┼───────────────────────────────┼──────────────────────────────────────────────────────┤
  │ 发送命令         │ connection.rs:send_command()  │ 把 JSON 塞到 Socket 里发给 daemon                    │
  ├──────────────────┼───────────────────────────────┼──────────────────────────────────────────────────────┤
  │ 格式化输出       │ output.rs                     │ 把 daemon 返回的原始 JSON 变成好看的样子             │
  ├──────────────────┼───────────────────────────────┼──────────────────────────────────────────────────────┤
  │ 批量执行         │ main.rs:run_batch()           │ 如果 Agent 用 batch，循环执行上面的流程              │
  └──────────────────┴───────────────────────────────┴──────────────────────────────────────────────────────┘

  ---
  通俗类比
  
  - CLI = 餐厅服务员：你（Agent）点菜，他记菜单（解析 JSON），传给后厨（Daemon），再把菜端上来（格式化输出）。
  - Daemon = 后厨大厨：真正开火炒菜（操作 Chrome），一直站在灶台前（持久化 session）。
  - CDP = 大厨跟灶台之间的煤气管道：Chrome 只认这个协议。

  Agent 全程不需要知道 Chrome 在哪、怎么启动、DOM 怎么选。它只需要说「点 @e5」和「截个图」，CLI 和 Daemon 帮它搞定一切。