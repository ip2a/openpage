# openpage

Pure Rust browser automation library for OpenPage.

This crate is the programmable API surface. The `openpage` command-line application lives separately under `rust/apps/openpage` and depends on this crate.
