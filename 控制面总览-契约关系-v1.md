# 控制面总览：契约关系 v1

日期：2026-06-05

范围：

- `openpage doctor`
- `openpage browser list`
- `openpage browser status --session ...`
- `openpage browser logs --session ...`

目的：给上层调用方一个“一页纸”视角，明确这些控制面输出之间：

- 哪些字段是共享的
- 哪些字段只属于某一个 surface
- 推荐如何组合消费

---

## 1. 这四个 surface 的职责分工

### 1.1 `doctor`

职责：

- 做本机环境诊断
- 做 daemon sidecar 诊断
- 暴露自动可修项
- 在 `--fix` 时返回 applied actions

更适合：

- 启动前自检
- 诊断失败原因
- 自动修复入口

### 1.2 `browser list`

职责：

- 给出当前 `OPENPAGE_HOME` 下 daemon session 的全局列表视图
- 提供 summary 级数量统计

更适合：

- 扫描当前所有 session
- 全局状态看板
- 批量发现 incompatible / incomplete / cleaned 项

### 1.3 `browser status`

职责：

- 给出单个 session 的当前 daemon 运行态

更适合：

- 针对某个 session 做精确判断
- 作为后续动作前的状态门卫

### 1.4 `browser logs`

职责：

- 在 `browser status` 机器字段基础上附加日志存在性和日志内容

更适合：

- 诊断单个 session 的 daemon 异常
- 从状态跳转到日志证据

---

## 2. 当前共享的稳定机器字段

### 2.1 daemon session 主类型

当前这四个控制面里，凡是“具体 daemon session”视角，已经对齐到：

- `kind="daemon_session"`

适用范围：

- `doctor checks[]` 中具体 daemon session 检查
- `browser list` 的 `sessions[]`
- `browser list` 的 `incomplete[]`
- `browser list` 的 `cleaned[]`
- `browser status` 顶层 payload
- `browser status` 的嵌套 `incomplete`
- `browser logs` 顶层 payload

### 2.2 共享状态字段

以下字段现在是跨 surface 的共享基线：

- `state`
- `reasons`
- `fix`
- `log_path`
- `log_exists`

说明：

- `state` / `reasons` 给机器判断
- `fix` 给下一步动作建议
- `log_path` / `log_exists` 给日志诊断入口

### 2.3 当前共享 state 集合

当前稳定状态值：

- `healthy`
- `incompatible`
- `incomplete`
- `inactive`
- `cleaned`

注意：

- `inactive` 主要出现在单 session 视图（`status` / `logs`）
- `cleaned` 主要出现在全局列表视图（`list.cleaned[]`）和 `doctor` 的 cleaned daemon 检查

### 2.4 当前共享 reasons 集合

当前稳定原因码：

- `version_mismatch`
- `missing_pid`
- `invalid_pid`
- `missing_port`
- `invalid_port`
- `missing_version`
- `daemon_not_ready`
- `not_alive`

---

## 3. doctor 独有的稳定字段

`doctor` 独有能力：

### 3.1 summary

- `pass`
- `warn`
- `fail`
- `info`
- `fixable`
- `total`
- `warn_ids`
- `fail_ids`
- `info_ids`
- `fixable_ids`

### 3.2 checks[]

`doctor checks[]` 独有的稳定补充：

- `category`
- 更丰富的 `kind` taxonomy
  - `openpage_home`
  - `daemon_dir`
  - `legacy_sessions`
  - `daemon_sessions`
  - `daemon_session`
  - `browser_config`
  - `browser_executable`
  - `browser_launch`
- `auto_fixable`

### 3.3 fixed[]

`--fix` 时独有：

- `check_id`
- `message`
- `auto_fixable`
- `source`
- `reason`
- `session?`
- `path?`

这是 applied actions 历史，不是当前状态。

---

## 4. browser daemon surfaces 独有字段

### 4.1 `browser list`

独有：

- `summary.healthy`
- `summary.incompatible`
- `summary.incomplete`
- `summary.cleaned`
- `summary.total`

以及按列表类别区分：

- `sessions[]`
- `incomplete[]`
- `cleaned[]`

### 4.2 `browser status`

独有：

- 单 session 顶层视图
- 在 `state="incomplete"` 时带嵌套 `incomplete`

### 4.3 `browser logs`

独有：

- `tail`
- `content`
- `path`（兼容别名）
- `exists`（兼容别名）

说明：

- 新代码优先消费 `log_path` / `log_exists`
- `path` / `exists` 只作为兼容别名

---

## 5. `--fix` 语义与浏览器 surface 的关系

### 5.1 doctor 的视图

`doctor --quick --fix` 的当前稳定语义：

- `fixed[]` = 刚刚执行过的修复动作
- `summary / checks / inventory` = 修复后的当前状态

### 5.2 browser surface 的视图

`browser list / status / logs` 没有 applied-actions 历史语义。  
它们只表达当前 daemon 运行态。

所以：

- 要看“做了什么” → 用 `doctor --fix` 的 `fixed[]`
- 要看“现在是什么状态” → 用 `browser list / status / logs`

---

## 6. 推荐消费顺序

### 6.1 全局巡检

1. 先跑 `doctor --quick`
2. 看 `summary.fail_ids` / `summary.fixable_ids`
3. 如果需要全局 session 视角，再看 `browser list`

### 6.2 单 session 诊断

1. 先看 `browser status --session ...`
2. 按 `kind="daemon_session"` 确认 payload 类型
3. 看 `state`
4. 看 `reasons`
5. 需要证据时再看 `browser logs --session ...`

### 6.3 自动修复

1. 先看 `doctor.summary.fixable_ids`
2. 再跑 `doctor --quick --fix`
3. 用 `fixed[]` 读取 applied actions
4. 如需确认结果，再回读：
   - `doctor` 的 post-fix `summary / checks / inventory`
   - 或 `browser list / status`

---

## 7. 当前阶段建议

当前阶段，上层自动化应优先依赖：

- `doctor-契约盘点-v1.md`
- `doctor-契约收口结论-v1.md`
- `browser-daemon-契约盘点-v1.md`

这份总览文档只做关系图，不替代那三份细节文档。
