# OpenPage 学习 Microsoft Playwright 计划

## 一、目标与边界

OpenPage 不复制 Playwright，而是吸收其在 Chromium 自动化中的成熟设计，形成：

> Chromium-first、Rust-first、Python 薄门面、适合脚本与 Agent 的浏览器自动化 SDK。

核心边界：

- 只重点支持 Google Chrome / Chromium；
- Rust 是唯一领域模型和行为来源；
- Python 只提供 PyO3 薄门面；
- 顶层优先保持 `Browser / Page / Session`；
- 不增加 Helper、Adapter、Runtime 产品层、兼容层、废弃别名或 fallback；
- 不复制 Playwright Test、Fixture、Reporter；
- Context 隔离已完成研究，但暂不进入当前实施计划。

## 二、主要差距

| 环节 | OpenPage 当前状态 | 值得学习的能力 | 优先级 |
|---|---|---|---|
| 动作与等待 | 已有点击、输入和显式等待 | 操作内部自动完成存在、唯一、可见、稳定、未遮挡、可交互检查 | P0 |
| Element 稳定性 | `find()` 返回实时 `Element` | 动态 DOM 中安全地重新确认目标，避免 stale element | P0 |
| 语义定位 | 主要使用 CSS/XPath | role、text、label、placeholder、test id | P0 |
| 网络控制 | Rust 已有部分能力，Python 门面尚未完整公开 | 请求/响应监听、失败事件、拦截、继续、终止和 Mock | P1 |
| 错误诊断 | 已有错误返回 | 错误携带操作、定位条件、URL、超时和元素状态 | P1 |
| Chrome 专项能力 | 已有标签页、Cookie、上传、下载、截图等能力 | 继续强化 iframe、Shadow DOM、CDP 和页面生命周期 | P2 |

## 三、正式执行计划

### 阶段一：统一动作可靠性与等待语义

目标：让等待成为 Rust 动作语义的一部分，不要求 Python 用户手工编排。

覆盖：

```python
page.goto(url)
page.click(locator)
page.input(locator, text)
element.click()
element.input(text)
element.submit()
element.hover()
```

Rust Core 内部统一完成：

1. 等待元素存在；
2. 检查匹配唯一性；
3. 检查元素可见；
4. 检查元素启用；
5. 检查元素稳定；
6. 检查元素是否被遮挡；
7. 检查元素能否接收对应动作；
8. 在明确的超时范围内重试；
9. 返回结构化错误。

超时模型：

| 项目 | 设计要求 |
|---|---|
| 默认操作超时 | 由 Rust 统一管理 |
| 页面加载超时 | 与元素操作超时分离 |
| 单次覆盖 | 稳定后允许 `timeout_ms` 参数 |
| 单位 | Python/Rust 统一使用毫秒 |
| 超时错误 | 包含操作、定位条件、URL、等待状态和最终原因 |

验收标准：

```python
page.click("#submit")
```

正常情况下无需预先调用：

```python
page.wait_for("#submit")
```

不增加 `WaitManager`、`ActionHelper` 或 Python 编排层。

当前进度（2026-07-24）：

| 子项 | 状态 | 结果 |
|---|---|---|
| 点击失败语义 | 已完成 | 删除 `raise_when_click_failed`，点击失败始终返回错误 |
| JavaScript 点击边界 | 已完成 | 仅 `by_js=true` 时允许 JavaScript 点击，不再隐式回退 |
| 点击前状态检查 | 已完成 | 检查位置、稳定、可见、启用、视口和遮挡状态 |
| 真实 Chromium 失败路径 | 已完成 | 正常点击成功；隐藏及被遮挡元素明确失败且未触发事件 |
| 输入动作可靠性 | 已完成 | 输入前检查可交互状态；聚焦失败不再回退点击；非 JS 清空不再回退脚本赋值 |
| 提交、悬停统一语义 | 已完成 | submit 仅使用 Chrome `requestSubmit()`；hover 检查可见、稳定、视口和遮挡状态 |
| 单次 `timeout_ms` 门面 | 已完成 | Page/Element 的点击、输入、清空和悬停支持单次毫秒超时，等待预算由 Rust 统一消费 |
| 结构化动作错误 | 已完成 | Rust 动作错误携带 operation、locator、URL、timeout、匹配数量、元素状态和失败原因 |

### 阶段二：Element 动态 DOM 稳定性

目标：保留 `Element` 名称和直观语义，同时增强动态页面中的可靠性。

保留：

```python
element = page.find("#content")
child = element.find(".child")
child.click()
```

需要实现：

- Element 保存足够的原始定位信息；
- 链式查询保留父级查询作用域；
- 每次动作前验证节点是否仍然有效；
- 原节点失效时，仅在目标仍可无歧义识别时重新查询；
- 区分“元素找不到”“原节点失效”和“重新定位结果有歧义”；
- 不新增公开 `Locator` 类型。

当前进度（2026-07-24）：

| 子项 | 状态 | 结果 |
|---|---|---|
| 删除旧 `ele` 命名 | 已完成 | Rust API、PyO3 调用、CLI/daemon 正式协议统一使用 `element` / `elements`，不保留旧别名或双协议分支 |
| Element 定位元数据 | 已完成 | Rust `Element` 保存由 `Page.find()` / `Element.find()` 形成的完整唯一查询路径 |
| 节点有效性验证 | 已完成 | 点击、输入、清空、提交、悬停、按键、聚焦、滚动、拖拽、上传和属性修改等操作先验证原节点 |
| 安全唯一重定位 | 已完成 | 查询路径逐级要求恰好匹配一个元素；0 个、多个和无查询来源分别明确失败 |
| 动态 DOM 验证 | 已完成 | 覆盖 CSS/XPath 重渲染成功、零匹配、歧义匹配及 `find_all()` 节点失效场景 |

#### 安全重定位边界

重新定位是正式动作语义，不是 fallback。

允许重新定位：

```text
原定位条件仍唯一匹配同一语义目标
父级作用域仍有效
页面重渲染仅替换了 DOM 节点
```

必须报错：

```text
定位条件匹配多个元素
find_all() 返回列表后顺序发生变化
无法证明新节点仍是原目标
父级作用域已经发生歧义
```

原则：

> 允许安全重定位，拒绝有歧义的自动恢复。

验收标准：页面局部重渲染后，在定位语义仍然唯一成立时，Element 可以继续操作；不能唯一确认目标时必须明确失败。

正式边界：

- 仅 `Page.find()` 与基于唯一父路径的 `Element.find()` 产生可重定位查询路径；
- `find_all()` 返回值不依据新列表位置恢复，节点失效后返回 `ElementDetached`；
- 路径任一级匹配 0 个元素返回 `ElementNotFound`；
- 路径任一级匹配多个元素返回 `ElementAmbiguous`；
- Python 不保存定位元数据，不执行节点验证、等待或重定位。

### 阶段三：语义定位

目标：保留 CSS/XPath，同时增加面向用户语义的稳定定位方式。

继续保留：

```python
page.find("#submit")
page.find(".item")
page.find("//button")
```

正式门面继续统一使用 `find()`，不扩张顶层方法数量：

```python
page.find("role=button[name='登录']")
page.find("text=提交")
page.find("label=用户名")
page.find("placeholder=请输入用户名")
page.find("testid=submit")
```

Element 使用同一套语法完成作用域内查询：

```python
page.find("role=dialog").find("text=确认")
```

当前进度（2026-07-24）：

| 子项 | 状态 | 结果 |
|---|---|---|
| 文本定位 | 已完成 | `text=` 定位包含目标文本的最小元素 |
| 角色定位 | 已完成 | `role=` 支持显式角色、常用 HTML 隐式角色及可选精确名称 |
| 标签定位 | 已完成 | `label=` 支持包裹控件和 `for` / `id` 关联控件 |
| 占位符定位 | 已完成 | `placeholder=` 使用精确属性匹配 |
| 测试标识定位 | 已完成 | `testid=` 对应 `data-testid` 精确匹配 |
| Page/Element 统一作用域 | 已完成 | 两者复用 Rust `Locator::parse()`，Python 不包含定位实现 |
| 验证 | 已完成 | Rust 解析测试和真实 Chromium Page/Element 查询通过 |

正式契约：默认精确匹配 role 名称、label、placeholder 和 testid；`text=` 保留包含匹配。当前不引入正则、大小写选项或公开 `Locator` 对象。

实施原则：

- 定位逻辑进入 Rust Core；
- Python 只暴露调用签名；
- 不增加 LocatorBuilder、SelectorHelper；
- 不一次性复制 Playwright 全部 Locator API；
- 每种定位方式必须有 Rust 测试和真实 Chrome 验证；
- 名称、匹配范围及严格性必须先形成统一契约。

### 阶段四：网络监听与拦截

目标：从 Rust Core 直接公开稳定的页面网络能力。

正式门面：

```python
listener = page.listen()
listener.start(targets=["/api"], methods=["GET"])
packet = listener.wait(timeout_ms=2_000)
listener.stop()

interceptor = page.intercept()
interceptor.start(targets=["/api"])
request = interceptor.wait(timeout_ms=2_000)
request.continue_request()
request.abort()
request.fulfill(200, b"ok", {"content-type": "text/plain"})
interceptor.stop()
```

当前进度（2026-07-24）：

| 子项 | 状态 | 结果 |
|---|---|---|
| 请求、响应与失败监听 | 已完成 | `NetworkPacket` 公开 request、response 与 `is_failed` |
| 网络过滤 | 已完成 | 支持 URL、正则、method 与 resource type 过滤 |
| Response body | 已完成 | Python 以 `bytes` 读取 Rust listener 保存的响应体 |
| 请求继续 | 已完成 | `continue_request()` 支持原样继续及 URL、method、headers、post data 修改 |
| 请求终止 | 已完成 | Rust `InterceptedRequest.abort()` 是正式领域动作 |
| Mock 响应 | 已完成 | `fulfill()` 支持 status、body、headers 与 phrase |
| 真实 Chrome 验证 | 已完成 | listener、continue、abort、fulfill 均通过本地 HTTP E2E |

职责边界：

- `Listener`、`Interceptor`、packet 和 intercepted request 均由 Rust Core 定义；
- Python 只绑定对象、参数与返回值，不保存队列，不判断过滤条件，不执行网络行为；
- 网络对象不进入 `openpage` 顶层核心概念，由 `Page.listen()` / `Page.intercept()` 返回；
- 当前正式模型是同步 `start / wait / stop`，不并行增加 callback 事件系统；
- 不增加 Python `NetworkManager`、Helper、Adapter 或 Runtime；
- 不用轮询、兼容分支或 fallback 模拟网络事件。

Cookie 仍使用现有 Page/Session 各自的正式 cookie 能力；本阶段不增加隐式双向同步或新的交换管理层。

### 阶段五：错误与诊断

目标：自动化失败后能直接定位问题。

错误至少包含：

```text
operation
locator
url
timeout
matched_count
element_state
failure_reason
```

示例语义：

```text
click failed:
locator="#submit"
url="https://example.com/login"
timeout=10000ms
reason=element was found but remained hidden
```

当前进度（2026-07-24）：

| 子项 | 状态 | 结果 |
|---|---|---|
| Rust 诊断模型 | 已完成 | `ErrorDiagnostic` 是正式领域 payload，包含 operation、locator、url、timeout_ms、matched_count、element_state、failure_reason |
| 错误种类稳定性 | 已完成 | `OpenPageError::Diagnosed` 包装原始错误，Display 与 root kind 保持不变 |
| 动作诊断 | 已完成 | `click`、`fill`、`text`、`attr` 在 Rust 行为发生处附加定位与最终状态 |
| 导航诊断 | 已完成 | `goto` 在 Rust 附加 operation、URL、timeout 和失败原因 |
| daemon 协议 | 已完成 | `ResponseError` 序列化和反序列化完整保留全部诊断字段 |
| Python 薄映射 | 已完成 | `RuntimeError` 直接暴露稳定 `kind` 及七个可选诊断属性，不解析错误字符串 |
| 失败后显式取证 | 已完成 | `page.html()`、`page.snapshot()`、`page.save_screenshot()` 均通过真实 Chrome 验证 |
| 完整 Trace 平台 | 不实施 | 不在每个异常中隐式抓取 HTML、截图或 Document 大对象 |

职责边界：

- 诊断字段由 Rust Core 在动作和导航发生处生成；
- 协议层只负责无损传输，不推断诊断内容；
- Python 只把 Rust 字段设置到异常实例，不分类、不编排、不解析字符串；
- HTML、截图和 Document 快照继续由调用方在需要时显式获取，避免异常路径产生大 payload 和隐式副作用；
- 不增加 ErrorManager、TraceHelper、Adapter、Runtime、兼容错误别名或 fallback。

验证：Rust Core 343 项、openpage-app 195 项、Python 真实 Chrome E2E 1 项全部通过。

### 阶段六：Chrome 专项能力

继续审计和强化：

- iframe；
- Shadow DOM；
- 上传与下载；
- Popup 和标签页；
- JavaScript 对话框；
- Cookie 和 Storage；
- 网络拦截；
- Chrome DevTools Protocol；
- 截图和页面快照；
- 剪贴板和页面缩放；
- 页面加载及生命周期状态。

优先修复现有能力的正确性和稳定性，不为了数量扩充无明确需求的 API。

## 四、实施顺序

```text
1. 统一动作可靠性与等待语义
2. Element 动态 DOM 稳定性
3. 语义定位
4. 网络监听与拦截
5. 错误与诊断
6. Chrome 专项能力
```

每个阶段均遵循：

```text
确认 Rust 领域契约
→ 修改 Rust Core
→ Rust 测试
→ Python 薄门面
→ 真实 Chrome 验证
→ 更新文档
→ Git 提交
```

## 五、暂缓事项：Context 隔离

状态：**已研究，暂缓实施，不属于当前执行阶段。**

当前不新增或迁移以下公开门面：

```python
browser.new_context()
browser.new_page(isolated=True)
browser.new_page(new_context=True)
```

也不调整现有 API 来提前为 Context 铺路。

重新评估 Context 的触发条件：

1. 一个隔离登录环境需要同时管理多个 Page；
2. 同一 Browser 内运行多个账号；
3. 每个账号需要独立 Proxy、权限或网络规则；
4. 需要保存和恢复整组登录状态；
5. 需要一次关闭某个会话及其全部 Page；
6. 并行任务需要以隔离会话作为生命周期边界。

只有触发正式产品需求后，才重新确认是否采用：

```text
Browser
└── Context
    └── Page
```

在此之前保持：

```text
Browser
└── Page

Session
└── Response
```

## 六、明确不做

```text
Firefox 支持
WebKit 支持
Playwright Test 克隆
内置 Fixture 系统
内置 Reporter 系统
为了模仿而公开 BrowserType
当前阶段公开 BrowserContext
新增 Locator 顶层类型
Helper
Adapter
Runtime 产品层
兼容层
fallback
废弃别名
```

## 七、Playwright BrowserContext 学习记录

`BrowserContext` 是一个浏览器进程内部的独立浏览会话，可以理解为成本较低的无痕用户环境：

```text
Browser
├── BrowserContext A
│   ├── Page A1
│   └── Page A2
└── BrowserContext B
    └── Page B1
```

同一个 Context 内的 Page 共享会话环境，包括 Cookie、Storage、Cache、权限、User Agent、网络规则和登录状态。不同 Context 相互隔离；关闭 Context 会关闭其内部 Page 并释放相关资源。

典型关系：

```python
browser = chromium.launch()
context = browser.new_context()
page = context.new_page()
context.close()
browser.close()
```

### 它是不是 Playwright 的顶层概念

从领域模型看，它是正式公开、拥有独立 API 和生命周期的核心对象，不是内部实现细节。

从启动层级看，它不是最外层入口：

```text
Playwright
└── BrowserType
    └── Browser
        └── BrowserContext
            └── Page
```

因此准确表述是：

> BrowserContext 是 Playwright 的公开核心领域类型，是 Browser 下一级对象，但不是最外层启动入口。

## 八、最终决策

- Playwright 的 Context 设计合理，但解决的是浏览会话隔离和生命周期管理；
- OpenPage 当前没有足够需求证明必须增加公开 Context；
- 当前正式计划聚焦动作可靠性、Element 稳定性、语义定位、网络能力和错误诊断；
- Context 仅保留为学习记录和后续触发式决策，不进入本轮实现；
- 不为未来可能的 Context 预留抽象、参数或兼容入口；
- 后续若需求触发，必须重新进行 Rust/Python 顶层设计确认。
