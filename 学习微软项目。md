# OpenPage 学习 Microsoft Playwright 计划

## 一、目标与边界

OpenPage 不做 Playwright 的全量复制，而是吸收其在 Chromium 自动化中的成熟设计，形成：

> Chromium-first、Rust-first、Python 薄门面、适合脚本与 Agent 的浏览器自动化 SDK。

明确边界：

- 只重点支持 Google Chrome / Chromium；
- 不规划 Firefox 和 WebKit；
- Rust 是唯一领域模型和行为来源；
- Python 只提供 PyO3 薄门面；
- 不增加 Helper、Adapter、Runtime 产品层、兼容层或 fallback；
- 不为了对标 Playwright 而复制其测试运行器、Fixture 和 Reporter；
- 顶层设计优先保持 `Browser / Page / Session` 简洁模型。

## 二、当前主要差距

| 环节 | OpenPage 当前状态 | Playwright 值得学习的部分 | 优先级 |
|---|---|---|---|
| 动作可靠性 | 已有点击、输入、悬停、提交等操作 | 操作前自动完成存在、唯一、可见、稳定、未遮挡、可交互检查 | P0 |
| Element 稳定性 | `find()` 返回实时 `Element` | DOM 重渲染后可以重新定位，减少 stale element | P0 |
| 等待模型 | 已有显式等待方法 | 等待融入点击、输入、导航等动作，不要求用户手工编排 | P0 |
| 语义定位 | 主要依赖 CSS/XPath | role、text、label、placeholder、test id 等面向用户语义的定位 | P0 |
| 页面隔离 | 主要是 `Browser → Page`，部分隔离参数归属不够清晰 | 使用独立浏览上下文隔离 Cookie、Storage、权限和网络配置 | P1 |
| 网络控制 | Rust 已有部分网络能力，Python 门面尚未完整公开 | 请求监听、响应监听、失败事件、拦截、继续、终止和 Mock | P1 |
| 错误诊断 | 已有错误返回，但自动化现场信息仍可加强 | 错误携带操作、定位条件、URL、超时和元素状态 | P1 |
| Chrome 专项能力 | 已有标签页、Cookie、下载、上传、截图等能力 | 继续强化 iframe、Shadow DOM、CDP、弹窗及页面生命周期稳定性 | P2 |

## 三、执行计划

### 阶段一：动作可靠性

目标：现有动作在动态页面中稳定执行，不增加新顶层概念。

覆盖：

```python
page.click(locator)
page.input(locator, text)
element.click()
element.input(text)
element.submit()
element.hover()
```

Rust Core 内部应统一完成：

1. 等待元素存在；
2. 检查匹配唯一性；
3. 检查元素可见；
4. 检查元素启用；
5. 检查元素稳定；
6. 检查元素是否被遮挡；
7. 检查元素能否接收对应动作；
8. 在超时前按照统一规则重试；
9. 返回结构化错误。

验收标准：

```python
page.click("#submit")
```

正常情况下不要求用户预先调用 `page.wait_for()`。

### 阶段二：Element 动态稳定性

目标：保留 `Element` 名称和直观语义，同时增强动态 DOM 中的可靠性。

保留：

```python
element = page.find("#content")
child = element.find(".child")
child.click()
```

需要实现：

- Element 保存足够的原始定位信息；
- 链式查询保留父级查询作用域；
- 每次动作前验证节点是否仍然有效；
- 节点失效时按照原定位条件重新查询；
- 区分“元素找不到”和“原节点失效”；
- 不新增公开 `Locator` 类型。

验收标准：页面局部重渲染后，在定位语义仍然成立的情况下，Element 可以继续完成操作。

### 阶段三：统一等待模型

目标：等待是 Rust 动作语义的一部分，而不是 Python 用户必须编排的步骤。

统一覆盖：

```python
page.goto(...)
page.click(...)
page.input(...)
element.click(...)
element.input(...)
element.hover(...)
element.submit(...)
```

需要明确：

| 项目 | 要求 |
|---|---|
| 默认操作超时 | 由 Rust 统一管理 |
| 页面加载超时 | 与元素操作超时分离 |
| 单次覆盖 | 稳定后允许 `timeout_ms` 参数 |
| 超时值 | 使用毫秒，保持 Python/Rust 一致 |
| 超时错误 | 包含操作、定位条件、URL、等待状态和最终失败原因 |

不增加 `WaitManager`、`ActionHelper` 或编排类。

### 阶段四：语义定位

目标：在保留 CSS/XPath 的基础上，增加稳定、面向用户语义的定位方式。

继续保留：

```python
page.find("#submit")
page.find(".item")
page.find("//button")
```

候选门面：

```python
page.find_role("button", name="登录")
page.find_text("提交")
page.find_label("用户名")
page.find_placeholder("请输入用户名")
page.find_test_id("submit")
```

Element 同样支持作用域内查询：

```python
page.find_role("dialog").find_text("确认")
```

原则：

- 定位逻辑进入 Rust Core；
- Python 只暴露调用签名；
- 不增加 LocatorBuilder、SelectorHelper 等类型；
- 不一次性复制 Playwright 全部 Locator API；
- 每增加一种定位方式，都必须有真实页面验证。

### 阶段五：页面隔离语义

目标：在不急于增加公开 `BrowserContext` 类型的前提下，先纠正能力归属。

当前需要重新审视：

```python
page.new_page(new_context=True)
```

创建隔离浏览环境属于 Browser，而不是 Page。短期候选设计：

```python
browser.new_page(isolated=True)
```

或者：

```python
browser.new_page(new_context=True)
```

隔离范围至少包括：

- Cookie；
- Local Storage；
- Session Storage；
- Cache；
- User Agent；
- Proxy；
- 权限；
- 下载设置；
- 页面生命周期。

只有当 OpenPage 明确需要管理多账号、多代理、多用户并行以及一个隔离环境内的多个 Page 时，再决定是否公开 `Context`。

### 阶段六：网络监听和拦截

目标：从 Rust Core 直接公开稳定的页面网络能力。

候选能力：

```python
page.on_request(...)
page.on_response(...)
page.on_request_failed(...)
page.intercept(...)
page.abort(...)
page.continue_request(...)
page.fulfill(...)
```

覆盖：

- 请求和响应监听；
- 请求失败事件；
- URL/资源类型过滤；
- 请求终止；
- 请求继续及参数修改；
- Mock 响应；
- Cookie 与 Session 的明确交换能力。

不增加 Python 侧 NetworkManager、InterceptorAdapter 或回退处理层。

### 阶段七：错误和诊断

目标：自动化失败后可以直接定位问题，不依赖猜测。

错误信息至少包含：

```text
operation
locator
url
timeout
matched_count
element_state
failure_reason
```

示例语义：

```text
click failed:
locator="#submit"
url="https://example.com/login"
timeout=10000ms
reason=element was found but remained hidden
```

优先做好单次操作错误、截图、HTML 和 Document 快照，不立即建设完整 Trace 平台。

### 阶段八：Chrome 专项能力

继续强化：

- iframe；
- Shadow DOM；
- 下载与上传；
- Popup 和标签页；
- JavaScript 对话框；
- Cookie 和 Storage；
- 网络拦截；
- Chrome DevTools Protocol；
- 截图和页面快照；
- 剪贴板和页面缩放；
- 页面加载及生命周期状态。

## 四、推荐实施顺序

```text
1. 动作可靠性
2. Element 动态稳定性
3. 统一等待模型
4. 语义定位
5. 页面隔离语义
6. 网络监听与拦截
7. 错误和诊断
8. Chrome 专项能力
```

前四项决定 OpenPage 的基础使用体验，应优先于新增大量 API。

## 五、明确不做

```text
Firefox 支持
WebKit 支持
Playwright Test 克隆
内置 Fixture 系统
内置 Reporter 系统
为了模仿而公开 BrowserType
过早公开 BrowserContext
新增 Locator 顶层类型
Helper
Adapter
Runtime 产品层
兼容层
fallback
废弃别名
```

## 六、Playwright BrowserContext 是什么

`BrowserContext` 是一个浏览器进程内部的独立浏览会话，可以理解为成本较低的无痕用户配置环境。

关系如下：

```text
Browser
├── BrowserContext A
│   ├── Page A1
│   └── Page A2
└── BrowserContext B
    └── Page B1
```

同一个 Context 内的 Page 共享：

- Cookie；
- Local Storage；
- Session Storage 所属会话环境；
- Cache；
- 权限；
- User Agent 等上下文配置；
- 网络监听和拦截规则；
- 登录状态。

不同 Context 之间的数据相互隔离。关闭 Context 会一次性关闭并释放其内部页面和相关资源。

典型使用方式：

```python
browser = chromium.launch()
context = browser.new_context()
page = context.new_page()
page.goto("https://example.com")
context.close()
browser.close()
```

## 七、BrowserContext 是否是顶层概念

需要区分两个含义。

### 在 Playwright 领域模型中：是

`BrowserContext` 是 Playwright 正式公开、拥有独立 API 和生命周期的核心领域对象。用户可以直接创建、持有、配置和关闭它。因此它属于 Playwright 的主要公开概念，而不是内部实现细节。

### 在 Python 包导入层面：不是最外层启动入口

用户通常不会从 BrowserContext 开始创建对象，而是：

```text
Playwright → BrowserType → Browser → BrowserContext → Page
```

所以它是公开核心类型和 Browser 下一级领域对象，但不是类似 `chromium.launch()` 的最初入口。

## 八、OpenPage 是否应该立即公开 Context

当前结论：不立即公开。

OpenPage 当前模型：

```text
Browser
└── Page

Session
└── Response
```

在多数单页面脚本和 Agent 场景中，Context 可以先作为 Browser 创建隔离 Page 时的内部资源：

```python
page = browser.new_page(isolated=True)
```

这能保持顶层模型简洁。

当以下需求成为正式产品需求时，再公开 Context：

1. 一个隔离登录环境需要同时管理多个 Page；
2. 同一 Browser 内运行多个账号；
3. 每个账号需要独立 Proxy、权限或网络规则；
4. 用户需要单独保存和恢复整组登录状态；
5. 用户需要明确关闭某个会话及其全部 Page；
6. 并行任务需要以隔离会话作为生命周期边界。

达到这些条件后，合理模型应是：

```text
Browser
└── Context
    └── Page
```

在此之前，不应只因为 Playwright 有 `BrowserContext` 就增加一个公开类型。

## 九、当前设计结论

- Playwright 的 Context 设计本身是合理且成熟的；
- Context 解决的是浏览会话隔离和生命周期管理，不是普通页面查询问题；
- OpenPage 当前可以先吸收其隔离原则，而不复制其公开类型；
- 短期应把 `new_context` 能力从 Page 语义移动到 Browser；
- 是否公开 Context，应由多账号、多页面共享会话、独立代理和并行隔离需求决定；
- 不应为了 API 对标而增加没有明确用户价值的顶层概念。
