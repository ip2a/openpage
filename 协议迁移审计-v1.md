# [ARCHIVED] 协议迁移审计 v1

> 历史文档说明（2026-05-29）：本文件前半部分记录的是 **迁移开始时的审计现场**，
> 所以会保留当时仍然存在的 `serve --stdio`、`open_page()`、`load_session()` 等旧事实。
> 这些内容现在仅用于回溯，不代表当前实现。
>
> 当前权威状态请以：
> - `task_plan.md`
> - `notes.md`
> - `claude-progress.txt`
> - `rust/src/cli/connection.rs`
> - `rust/src/cli/serve.rs`
> - `rust/src/cli/oneshot.rs`
> 为准。
>
> 当前覆盖事实（2026-05-30）：
> - 当前活跃公开协议路径已经收敛到 **TCP daemon**
> - `serve --stdio` / one-shot attach / `page get` 一类旧表述都只是迁移起点的审计痕迹
> - 如果你要验证当前仓库，请不要执行这份文档里的旧命令，改用：
>   - `cargo run --manifest-path rust/Cargo.toml --bin openpage -- doctor --quick`
>   - `cargo run --manifest-path rust/Cargo.toml --bin openpage -- browser list`
>   - `skills/openpage-test/references/cli-smoke.md`
> - 这份文档的作用仅剩：解释迁移为什么开始、当时删除了什么、为什么要统一到 TCP

## 目标

回答三个问题：

1. OpenPage 当前本地到底有几套 CLI / daemon / protocol 路径
2. 哪些路径在与“唯一 TCP 协议”目标冲突
3. 下一步应该先删什么、先保什么、先接什么

---

## 当前本地代码现状

## 1. CLI 文件结构

当前 CLI 相关文件：

- `rust/src/cli/args.rs`
- `rust/src/cli/mod.rs`
- `rust/src/cli/oneshot.rs`
- `rust/src/cli/protocol.rs`
- `rust/src/cli/serve.rs`

这意味着当前 CLI 实际上至少分成了：

- 参数定义层
- 对外命令入口层
- oneshot 直连执行层
- server/daemon 层
- request/response 协议层

---

## 2. 当前并行存在的三套路径

## 路径 A：stdio daemon（已移除）

证据：

- `rust/src/cli/args.rs`
  - `ServeArgs { stdio: true, port: Option<u16> }`
- `rust/src/cli/serve.rs`
  - `run_stdio()`
- `README.md`
  - 明确写了 `cargo run ... serve --stdio`

判断：

- 这是当前仍然对外暴露的协议路径。
- 但它和“统一到 TCP”的目标直接冲突。

结论：

- **已从活跃代码与活跃文档中移除**

---

## 路径 B：TCP daemon

证据：

- `rust/src/cli/serve.rs`
  - `run_tcp(port)`
  - TCP listener / request dispatch
- `rust/src/cli/args.rs`
  - `ServeArgs.port`

判断：

- TCP 路径已经存在，但目前更像一个“备选 server 模式”，不是 CLI 的唯一执行路径。
- 目前没有看到完善的 connection 层、sidecar、ensure_daemon、自动发现机制。

结论：

- **这是应该升级为唯一主路径的候选**

---

## 路径 C：oneshot 直连浏览器

证据：

- `rust/src/cli/mod.rs`
  - `Command::Serve(args) => serve::run(args), command => oneshot::run(command)`
- `rust/src/cli/oneshot.rs`
  - 大量命令直接调用 `open_page(&args.session)?`
  - `load_session()`
  - `SessionRecord`
  - `Browser::connect(&record.debugger_url)?`

判断：

- 这说明当前用户主命令大量仍绕过 daemon，直接连浏览器。
- 因此现在的 TCP server 不是唯一真相源。

结论：

- **这是“唯一协议”目标下必须逐步收敛或替换的路径**

---

## 3. 当前协议层现状

证据：

- `rust/src/cli/protocol.rs`

现状：

- 已定义 `Request { id, op, target, params }`
- 已定义 `Response { id, ok, result, error }`

判断：

- 协议结构本身是清晰的。
- 不需要照搬竞品的 `action` 风格。
- 问题不在协议结构，而在 **调用链没有统一经过它**。

结论：

- **保留 `protocol.rs` 结构**
- **统一让 CLI 主路径最终都走它**

---

## 4. 当前与目标冲突最大的点

| 冲突点 | 当前状态 | 为什么有问题 |
|---|---|---|
| `serve --stdio` | 仍是公开入口 | 与“唯一 TCP”冲突 |
| `oneshot.rs` 大量直连 | 仍是默认执行路径 | daemon 不是唯一真相源 |
| 缺少 connection 层 | 还没有 daemon 发现/拉起/重试 | TCP 无法成为稳定主路径 |
| README 仍宣传 stdio | 文档与目标不一致 | 用户心智会继续分裂 |
| `serve.rs` 还只是 server | 不是 session-aware daemon 基础设施 | 无法支撑长期 CLI 收敛 |

---

## 5. 哪些本地实现应该保留

用户要求是：

- 元素定位
- 页面交互
- CDP 封装

**继续以 OpenPage 内部实现为准。**

因此下列模块应保持为内部执行真相，不从竞品复制：

- `rust/src/browser.rs`
- `rust/src/page.rs`
- `rust/src/element.rs`
- `rust/src/webpage.rs`
- `rust/src/session.rs`

---

## 6. 哪些竞品设计适合借入

只借 **非-CDP 外围设计**：

| 竞品能力 | 借鉴价值 | 是否建议 |
|---|---|---|
| daemon sidecar（`.pid/.port/.version`） | 高 | 建议 |
| `ensure_daemon()` | 高 | 建议 |
| `daemon_ready()` | 高 | 建议 |
| 请求重试与 transient error 分类 | 高 | 建议 |
| graceful shutdown / idle timeout | 高 | 建议 |
| AI-first snapshot 输出形态 | 中高 | 建议后续引入 |
| `RefMap` / `parse_ref()` | 中高 | 建议后续局部引入 |
| 输出截断 / boundary | 中 | 建议 |
| 竞品动作层 / CDP 封装 | 低 | 不建议 |

---

## 7. 下一步迁移动作

## 第一批

1. 新增 `rust/src/cli/connection.rs`
2. 把 daemon sidecar 机制加进来
3. 设计 OpenPage 的 `ensure_daemon(session)` 行为
4. 明确 `serve.rs` 未来只走 TCP，不再保留 stdio 作为主路径

### 当前进展

- 已新增：`rust/src/cli/connection.rs`
- 已接入：`serve` 写 `.port/.pid/.version`
- 已验证：`daemon.shutdown` 可以让 TCP daemon 退出并清理 sidecar
- 已删除：`serve --stdio`
- 已迁移第一批命令到 daemon 路径
- 尚未完成：CLI 全量命令仍未统一经过 daemon

## 第二批

1. 让 `oneshot.rs` 从直接连浏览器，逐步改为通过 `connection.rs` 发协议请求
2. 让 `serve.rs` 成为 CLI 的唯一长期执行后端

## 第三批

1. 删除或停用 `serve --stdio`
2. 清理 README / CLI help / 旧说明
3. 增加 AI 快照与输出增强

---

## 当前结论

### 已确认

- 当前公开 daemon 协议已统一为 **TCP**
- 当前仍存在部分 `oneshot` 直连路径，尚未完成“唯一执行真相”
- 最先需要持续做的是 **把剩余 `oneshot` 收敛进 daemon**
- 不应该去复制竞品的动作层或 CDP 实现

### 下一步原则

- 协议统一优先于新功能扩张
- 外围基础设施优先于动作层改写
- 保持 OpenPage 自己的元素/CDP/交互真相
