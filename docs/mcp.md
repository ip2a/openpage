# MCP

MCP is a mode of `openpage`, not a separate user-visible product.

The intended public shape is:

```bash
openpage
openpage mcp
openpage mcp serve
npx openpage
uvx openpage
```

Do not introduce `openpage-mcp` as a public command or package unless the product
strategy changes.
