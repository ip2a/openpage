# 借鉴迁移清单 v1

> 更新时间：2026-06-05  
> 范围：OpenPage 当前 **TCP daemon CLI** 的 shell/control-plane 借鉴执行清单  
> 前置约束：**只借控制面，不借 runtime 内核**

---

## 1. 目的

这份文档不再回答“值不值得借”，而是回答：

> **如果后面真的要 copy 一部分竞品代码过来微调，应该先抄哪一层、落到哪里、必须改掉什么、怎么验收。**

它是下面两份文档的执行版：

- `竞品文档-考虑借鉴的部分v1.md`
- `控制面地图-v1.md`

---

## 2. 总原则

| 规则 | 含义 |
|---|---|
| 只碰 `rust/src/cli/*` | 不碰 `browser/page/element/webpage` 真相源 |
| 只保留 TCP | 不重新引入 stdio / Unix socket / 第二协议面 |
| 借控制流，不借协议结构 | 不把竞品 `action/success/data` 结构抄进来 |
| 借机器语义，不借 runtime 行为 | 借 `state/reasons/fix/retryable`，不借 CDP / interaction |
| 每次借鉴都要带验证 | 至少有 grep / unit test / runtime smoke 其一，最好两者以上 |

---

## 3. 优先级总表

| 优先级 | 竞品来源 | OpenPage 落点 | 建议动作 | 结论 |
|---|---|---|---|---|
| P0 | `cli/src/connection.rs` | `rust/src/cli/connection.rs` | 可直接 copy 局部函数后微调 | 第一优先级 |
| P0 | `cli/src/doctor/mod.rs` + `doctor/launch.rs` | `rust/src/cli/doctor.rs` | 对模型和清理 guard 继续对齐 | 第一优先级 |
| P1 | `cli/src/output.rs` | `rust/src/cli/protocol.rs` | 按函数借 boundary / truncate / output shell | 适合小步吸收 |
| P1 | `cli/src/main.rs` | `rust/src/cli/mod.rs` + `rust/src/cli/oneshot.rs` | 只借顶层 JSON error shell 纪律 | 只借外壳 |
| P2 | `cli/src/commands.rs` | `rust/src/cli/args.rs` 或独立 util | 只挑小工具函数 | 可选 |
| P3 | `cli/src/native/*` | 无 | 禁止借 | 不要碰 |

---

## 4. 可直接迁移的清单

## 4.1 P0：`connection.rs`

这是最适合“直接 copy 一小段再微调”的地方。

### 建议迁移项

| 竞品函数/结构 | OpenPage 当前对应点 | 可否直接 copy | copy 后必须改什么 | 最小验收 |
|---|---|---|---|---|
| `cleanup_stale_files()` | `cleanup_sidecars()` / `force_cleanup_daemon()` | 可以局部借 | 改为 OpenPage 的 `.port/.pid/.version/.log` sidecar 布局 | unit test + synthetic `OPENPAGE_HOME` |
| `is_pid_alive()` | 同名能力已存在 | 可以对照微调 | 保持当前跨平台实现与错误语义 | unit test |
| `ActiveSession` | `DaemonSessionInfo` | 不建议整抄 | 保留 OpenPage 的 `alive/ready/version/log_path` 字段 | payload shape test |
| `CleanedSession` | `CleanedDaemonSession` | 可以参考 | 保留 OpenPage 的 `reason + reasons[]` 双表达 | payload shape test |
| `DaemonInventory` | `DaemonInventory` | 不建议整抄 | 不能丢 `incomplete[]` | `browser list` / `doctor inventory` |
| `walk_daemons()` | `daemon_inventory()` | 值得继续对标 | 保留 `sessions/incomplete/cleaned` 三分模型 | inventory tests |
| `ensure_daemon()` | `ensure_daemon()` | 值得持续对标 | 只保留 TCP；不要引入 socket 分支 | start/status/stop smoke |
| `send_command_once()` | `send_request_once()` | 可以借控制流 | 保留 OpenPage `Request/Response` 协议壳 | request round-trip test |
| `is_transient_error()` | `send_request_with_retry()` 的 transient 分类 | 值得继续对标 | 只扩展分类，不改 kind contract | transient retry tests |
| `send_command()` | `send_request()` / `send_request_existing()` | 可以参考 | 不引入竞品 `action` payload 结构 | runtime smoke |

### 迁移纪律

| 必须保留 | 不能回退 |
|---|---|
| `incomplete[]` | 不能退回只有 `sessions/cleaned` |
| `reasons[]` | 不能只剩人类可读 message |
| `fix` | 不能让 direct error 再退回只靠 message |
| TCP-only | 不能重新长出 stdio / socket 分支 |

### 当前最适合继续借的点

- `daemon_inventory()` 的扫描主循环纪律
- `send_request_with_retry(...)` 的 transient 分类边界
- `ensure_daemon(...)` 的重启/复用判断收口

---

## 4.2 P0：`doctor/mod.rs` + `doctor/launch.rs`

这一层适合继续“按模型对标”，不是大规模重写。

### 建议迁移项

| 竞品函数/结构 | OpenPage 当前对应点 | 可否直接 copy | copy 后必须改什么 | 最小验收 |
|---|---|---|---|---|
| `Status` | `enum Status` | 不需要整抄 | 保持现有 `pass/warn/fail/info` 即可 | summarize tests |
| `Check` | `struct Check` | 不需要整抄 | 保留当前 `session/state/reasons/browser_path/...` richer fields | doctor JSON shape |
| `Summary` | `struct Summary` | 可以持续对标 | 保留 `warn_ids/fail_ids/info_ids/fixable_ids` | summarize tests |
| `summarize()` | 同名函数已存在 | 可以对照微调 | 继续保持 fixable/total 统计稳定 | unit test |
| `print_json()` | `print_output_json(...)` + doctor payload | 只借思路 | 不做第二套输出路径 | doctor smoke |
| `LaunchGuard` | `BrowserLaunchGuard` / `TempDirGuard` | 值得继续对齐 | 保持当前 scratch session 和临时目录清理 | doctor full smoke |
| `send_json()` | doctor 内部最小 RPC 调用 | 只需借调用纪律 | 继续走 OpenPage `Request/Response` | smoke |

### 当前最适合继续借的点

- launch smoke 的失败收尾纪律
- doctor 内部临时资源清理一次性语义
- doctor check id 稳定性

### 不值得重做的点

- 当前 `Check` 字段已经比竞品更丰富，不要为了“像竞品”去删字段
- 当前 inventory 投影已经和 `browser list/status/logs` 对齐，不要拆成另一套语义

---

## 4.3 P1：`output.rs`

这层不适合整抄，适合继续吸收小函数。

### 建议迁移项

| 竞品函数 | OpenPage 当前对应点 | 建议动作 | 不能做什么 | 最小验收 |
|---|---|---|---|---|
| `get_boundary_nonce()` | `boundary_nonce()` | 继续保持 process-stable nonce contract | 不改成每次输出随机 | existing tests |
| `truncate_if_needed()` | `truncate_string()` | 持续对齐长文本截断行为 | 不破坏现有 JSON shell | output tests |
| `print_with_boundaries()` | `wrap_content()` | 继续吸收 origin-aware content wrapping | 不引入文本模式分叉 | runtime output smoke |
| `print_response_with_opts()` | `apply_output_filters()` / `print_output_json()` | 借“统一出口”心智 | 不加第二个 response schema | protocol tests |

### 当前最适合继续借的点

- boundary 包裹 contract
- origin-aware 输出标记
- 截断策略的一致性

---

## 4.4 P1：`main.rs`

这层只建议借“顶层壳纪律”。

### 建议迁移项

| 竞品函数 | OpenPage 当前对应点 | 建议动作 | 不该借的部分 | 最小验收 |
|---|---|---|---|---|
| `serialize_json_value()` | `print_output_json()` 上游序列化 | 只借统一出口纪律 | 不借整套 main 分发 | top-level error smoke |
| `print_json_value()` | `print_json(...)` / `print_output_json(...)` | 保持统一 JSON 打印出口 | 不增加第二套 helper 分叉 | unit test / smoke |
| `print_json_error()` | `simple_error*` | 继续收口顶层错误出口 | 不退回自由文本 | clap error smoke |
| `print_json_error_with_type()` | `error.kind` 体系 | 借稳定 kind 纪律 | 不引入竞品 error type 枚举 | invalid_input smoke |

### 当前最适合继续借的点

- top-level parse error 继续进统一 JSON shell
- direct CLI / daemon response / doctor 共享稳定错误语言

---

## 4.5 P2：`commands.rs`

这里只值得挑小工具，不值得当主线。

### 可选迁移项

| 竞品函数 | OpenPage 落点 | 建议 | 原因 |
|---|---|---|---|
| `ParseError` | `rust/src/cli/args.rs` | 只可参考分类方式 | 你当前主线不是 parser 重写 |
| `parse_curl_cookies()` | cookie import util | 如后续做 curl cookie 导入，可局部借 | 局部价值高，但不影响 TCP 控制面主线 |
| `gen_id()` | 无必要 | 不建议迁移 | OpenPage 当前不缺这层能力 |

---

## 5. 明确禁止迁移的清单

| 竞品区域 | 为什么不能借 | OpenPage 真相源 |
|---|---|---|
| `cli/src/native/actions.rs` | 会把控制面和 runtime 耦死 | `rust/src/browser.rs` / `page.rs` / `element.rs` / `webpage.rs` |
| `cli/src/native/browser.rs` | 会引入第二套浏览器生命周期真相 | 同上 |
| `cli/src/native/element.rs` | 会污染现有 locator/element 语义 | `rust/src/element.rs` |
| `cli/src/native/interaction.rs` | 会引入第二套交互语义 | `rust/src/page.rs` / `webpage.rs` |
| `cli/src/native/snapshot.rs` | 会污染现有 snapshot/ref 设计 | OpenPage 自己的 snapshot 能力 |
| `cli/src/native/*` 任何 CDP/stream/provider 实现 | 这不是你当前要借的层 | 当前 runtime 内核 |

判断标准很简单：

> 只要借鉴动作会让 `rust/src/cli/*` 以外的真相源发生迁移，这一步就偏题了。

---

## 6. 推荐执行顺序

| 顺序 | 动作 | 目标文件 | 验收方式 |
|---|---|---|---|
| 1 | 继续收紧 `connection.rs` inventory / retry / sidecar taxonomy | `rust/src/cli/connection.rs` | unit test + synthetic runtime |
| 2 | 继续收紧 direct daemon error round-trip | `rust/src/cli/protocol.rs` + `rust/src/cli/oneshot.rs` | protocol tests + CLI smoke |
| 3 | 继续收紧 doctor launch cleanup / check stability | `rust/src/cli/doctor.rs` | doctor tests + doctor smoke |
| 4 | 如有必要，再补 output boundary/truncate 细节 | `rust/src/cli/protocol.rs` | output tests |
| 5 | 最后才考虑 parser 小工具 | `rust/src/cli/args.rs` 等 | unit test |

---

## 7. 每次借鉴后的验收模板

每次真正 copy/微调一段代码后，至少回答这 5 个问题：

| 问题 | 通过标准 |
|---|---|
| 是否只改了 `rust/src/cli/*`？ | 是 |
| 是否仍然 TCP-only？ | 是 |
| 是否保留了 OpenPage 自己的 `Request/Response` 协议壳？ | 是 |
| 是否没有丢掉 `state/reasons/fix/retryable` 这些机器字段？ | 是 |
| 是否有对应的 test 或 runtime smoke 证明行为？ | 是 |

---

## 8. 当前结论

一句话说：

> 现在最值得 copy 的不是竞品的“浏览器能力”，而是它的 **CLI 控制面骨架**；最适合落地的不是大重构，而是继续在 `connection.rs`、`protocol.rs`、`doctor.rs` 这三层做小步收紧。

如果后面要继续推进，优先看：

- `rust/src/cli/connection.rs`
- `rust/src/cli/protocol.rs`
- `rust/src/cli/oneshot.rs`
- `rust/src/cli/doctor.rs`

而不是去碰：

- `rust/src/browser.rs`
- `rust/src/page.rs`
- `rust/src/element.rs`
- `rust/src/webpage.rs`
