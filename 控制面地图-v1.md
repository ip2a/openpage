# 控制面地图 v1

> 更新时间：2026-06-05  
> 范围：OpenPage 当前 **TCP daemon CLI** 的 shell/control-plane

---

## 1. 目的

这份文档回答的是：

> **当前 OpenPage CLI 的控制面到底由哪些模块组成，它们分别负责什么？**

这里不讨论浏览器 runtime 内核本身，而是讨论：

- daemon sidecar 真相源
- inventory / status / logs / doctor
- 顶层 JSON shell
- direct CLI 与 daemon response 的错误 round-trip

也就是这条最近几轮一直在收口的线。

当前最重要的源码真相源是：

- `rust/src/cli/connection.rs`
- `rust/src/cli/doctor.rs`
- `rust/src/cli/protocol.rs`
- `rust/src/cli/oneshot.rs`
- `rust/src/cli/mod.rs`

---

## 2. 一张总图

| 模块 | 角色 | 当前职责 |
|---|---|---|
| `cli/connection.rs` | daemon 控制面真相源 | sidecar 路径、inventory、daemon 生命周期、request retry、status/fix/reasons |
| `cli/doctor.rs` | 诊断面 | environment / daemon / browser checks，summary，`--quick` / `--fix` |
| `cli/protocol.rs` | JSON shell 与错误语义层 | `Request/Response`、`error.kind`、structured context、output filtering |
| `cli/oneshot.rs` | 命令壳 | 把用户命令映射到 request，消费 inventory/status/logs，打印统一 JSON |
| `cli/mod.rs` | 顶层入口 | clap parse、compat mode 分流、顶层错误出口 |

一句话说：

> `connection` 提供控制面真相，`doctor` 做诊断视图，`protocol` 定义 JSON 与错误语义，`oneshot` 负责命令落地，`mod` 负责最外层壳。

---

## 3. `connection.rs`：控制面真相源

这是当前最像竞品 `connection.rs` 的层，也是最值得继续借鉴的层。

### 3.1 它现在掌握什么真相

| 能力 | 说明 |
|---|---|
| `OPENPAGE_HOME/daemon` 目录布局 | 当前 sidecar 真相源 |
| `.port/.pid/.version/.log` | 当前 session 的 sidecar 状态 |
| daemon 是否 ready | 通过 TCP connect timeout 判断 |
| daemon inventory | 区分 `sessions / incomplete / cleaned` |
| session state | `healthy / incompatible / incomplete / inactive / cleaned` |
| fix guidance | `daemon_session_fix(...)` / `incomplete_daemon_fix(...)` |
| retry discipline | `send_request_with_retry(...)` |

### 3.2 当前最重要的数据模型

| 结构 | 作用 |
|---|---|
| `DaemonSessionInfo` | 活跃或可观测的 daemon session |
| `IncompleteDaemonSession` | sidecar 不完整但仍可能 alive 的 session |
| `CleanedDaemonSession` | 扫描时被清理掉的 stale residue |
| `DaemonInventory` | 当前控制面 inventory 顶层对象 |

### 3.3 现在已经稳定下来的 machine-readable 语义

| 字段 | 当前意义 |
|---|---|
| `state` | session 当前控制面状态 |
| `reasons[]` | 为什么落到这个状态 |
| `fix` | 推荐恢复动作 |
| `version_matches_current_cli` | version mismatch 是否成立 |
| `ready` / `alive` | daemon 连接态与进程态 |

### 3.4 这一层和竞品的关系

当前 OpenPage 这层已经不是简单“照着竞品抄”，而是：

- 保留了竞品那种 lifecycle / sidecar / retry / inventory 的控制面思路
- 但已经发展出更细的：
  - `incomplete`
  - `cleaned`
  - `reasons[]`
  - `fix`

所以后续如果再借鉴，原则应该是：

> **借控制流纪律，不要回退掉 OpenPage 现有 richer inventory model。**

---

## 4. `doctor.rs`：诊断面

`doctor` 当前不是装饰命令，而是控制面状态的诊断视图。

### 4.1 当前 doctor 在检查什么

| 类别 | 说明 |
|---|---|
| Environment | `OPENPAGE_HOME`、daemon 目录、legacy session JSON residue |
| Daemon | inventory、cleaned residue、incomplete session、healthy/incompatible session |
| Browser | browser config resolution、browser executable resolution、launch smoke |

### 4.2 当前 doctor 输出有什么特点

| 特点 | 说明 |
|---|---|
| `checks[]` | 稳定检查项 |
| `summary` | `pass/warn/fail/info/fixable` 统计 |
| `inventory` | 当前 daemon runtime truth 的镜像 |
| `fixable_ids` | 自动化友好 |
| `browser_path / resolved_path / suggested_path` | browser 配置定位更机器可读 |

### 4.3 当前已经和 connection 层对齐的点

| 对齐点 | 说明 |
|---|---|
| `state` | 与 `browser status/list/logs` 同一状态语言 |
| `reasons[]` | 与 inventory / direct error 同一 taxonomy |
| `fix` | 与 connection 层共享恢复指导 |
| `cleaned.reasons[]` | stale residue 现在也带稳定 taxonomy |

### 4.4 阶段性判断

当前 `doctor.rs` 已经不是主要缺口。  
它更像是：

> **connection 真相源的结构化诊断投影。**

后续更值得做的通常不是“大改 doctor”，而是：

- 收紧 contract
- 保持与 connection / protocol 同语义

---

## 5. `protocol.rs`：JSON shell 与错误语义层

这一层决定：

- JSON payload 长什么样
- `error.kind` 如何归类
- structured daemon error 如何 round-trip 回来
- output boundaries / truncation 如何做

### 5.1 当前它负责的三件大事

| 能力 | 说明 |
|---|---|
| `Request/Response/ResponseError` | CLI ↔ daemon 的协议壳 |
| `simple_*` / `response_*` | JSON shell 统一出口 |
| `openpage_error_kind(...)` | 错误分类规则 |

### 5.2 目前已稳定的错误语义收口

当前已经明确分层：

- 输入坏了 → `invalid_input`
- 功能/平台限制 → `unsupported_operation`
- 运行时状态不对 → `browser_operation`
- daemon 可重试短暂故障 → `daemon_transient`

详细规则已经单独写在：

- `错误语义地图-v1.md`

### 5.3 当前这层最重要的价值

它让：

- top-level CLI parse error
- batch malformed input
- direct invalid-value checks
- daemon-side parameter validation

尽量说成同一种机器语言。

也让：

- `session`
- `state`
- `reasons[]`
- `retryable`
- `suggested_action`
- `fix`

能跨 daemon response round-trip 保留下来。

这一层现在也开始覆盖一小类**非 daemon 的 workflow restriction fix**，例如：

- removed legacy surface parse error 的迁移 `fix`
- `batch cannot execute ...` 这类 restriction 的 top-level workflow `fix`

---

## 6. `oneshot.rs`：命令壳

`oneshot` 不是控制面真相源，但它是最主要的控制面消费层。

### 6.1 它在做什么

| 能力 | 说明 |
|---|---|
| command -> request | 把 CLI 命令映射成 daemon request |
| bootstrap | 某些命令会保证 session/webpage 存在 |
| browser inventory/status/logs | 消费 connection 层真相并输出 |
| batch | 批量执行与错误聚合 |
| print_json | 所有用户面最终仍回到统一 JSON shell |

### 6.2 它和控制面的关系

| 场景 | 真相源 |
|---|---|
| `browser list` | `connection.daemon_inventory_payload_json(...)` |
| `browser status` | `connection.daemon_status_payload_for_session(...)` |
| `browser logs` | `browser status` + log file content |
| direct daemon response error | `response_result(...)` -> `protocol.openpage_error_from_response_error(...)` |

### 6.3 这一层最近几轮收口的重点

| 收口点 | 说明 |
|---|---|
| batch malformed input | 统一进 `invalid_input` |
| direct daemon structured error | 保留 `session/state/reasons/fix/...` |
| browser inventory/logs/status | 共用同一状态语言 |

---

## 7. `mod.rs`：最外层壳

`mod.rs` 很薄，但很关键。

### 7.1 当前职责

| 能力 | 说明 |
|---|---|
| 顶层 clap parse | 判断命令是否有效 |
| compat surface 分流 | `dp` compat mode 与主 CLI 分离 |
| 顶层错误出口 | parse error -> JSON shell |
| command dispatch | `serve` / `doctor` / `oneshot` |

### 7.2 这一层的重要纪律

- help/version 继续保留 clap 文字输出
- 其他输入错误尽量走 machine-friendly JSON shell
- 主 CLI 继续坚持：
  - **TCP-backed daemon only**

---

## 8. 当前控制面数据流

可以把当前控制面主路径理解成：

1. `mod.rs`
2. `oneshot.rs` / `doctor.rs`
3. `connection.rs`
4. `protocol.rs` 输出 JSON shell

更具体一点：

### 8.1 `browser list`

`oneshot::run_browser(List)`
→ `connection::daemon_inventory()`
→ `connection::daemon_inventory_payload_json(...)`
→ `protocol::print_output_json(...)`

### 8.2 `doctor --quick`

`doctor::run(...)`
→ `environment_checks(...)`
→ `daemon_checks(...)`
→ `browser_checks(...)`
→ `inventory + checks + summary`
→ `protocol::print_output_json(...)`

### 8.3 direct daemon error

daemon returns `ResponseError`
→ `oneshot::response_result(...)`
→ `protocol::openpage_error_from_response_error(...)`
→ `protocol::simple_openpage_error(...)`
→ final JSON shell

---

## 9. 当前阶段性结论

到 2026-06-04 这个节点，OpenPage 的控制面可以概括成：

### 已经比较稳的部分

- TCP-only daemon sidecar model
- inventory / status / logs / doctor 的同语义状态语言
- `error.kind` 的 shell/control-plane 收口
- structured daemon error round-trip
- contract tests 对当前错误边界的保护

### 最值得继续借鉴竞品的部分

- `connection.rs` 的控制流纪律
- `doctor` 的诊断契约纪律
- 输出壳的一致性

### 不该再动回去的部分

- 不要重新引入第二协议真相
- 不要把 richer inventory model 简化回去
- 不要把已经统一的错误语义再漂散

一句话说：

> **当前 OpenPage 的控制面已经形成了自己的真相层，竞品现在更适合作为“继续收紧纪律”的参照，而不是“整层照搬”的模板。**
