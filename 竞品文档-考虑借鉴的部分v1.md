# 竞品文档：考虑借鉴的部分 v1

> 更新时间：2026-06-01  
> 参考项目：`/Volumes/data0/data4work/2026_5/openpage/参考项目/agent-browser-main`

## 1. 一句话结论

在你已经明确 **OpenPage 只走 TCP daemon** 的前提下，这个竞品最值得借的是：

> **daemon 控制面、doctor、自描述输出、顶层错误壳、agent 文档组织。**

不要借的是：

> **浏览器/CDP/locator/interaction/snapshot runtime 内核。**

也就是说，可以把 `agent-browser` 当成一个 **browser automation CLI 的外壳工程样板**，而不是运行时内核样板。

---

## 2. 本次分析边界

### 2.1 你的既定边界

| 维度 | 当前决策 | 对竞品的态度 |
|---|---|---|
| 通信模式 | **TCP-only** | 只借 daemon 生命周期和 sidecar 思路 |
| 协议结构 | 保留 OpenPage 自己的 `Request/Response` | 不借竞品 `action/success/data` 结构 |
| 浏览器封装 | 保留 OpenPage 自己的实现 | 不借 |
| CDP 封装 | 保留 OpenPage 自己的实现 | 不借 |
| locator / element | 保留 OpenPage 自己的实现 | 不借 |
| interaction runtime | 保留 OpenPage 自己的实现 | 不借 |
| snapshot 底层抓取 | 保留 OpenPage 自己的实现 | 不借 |
| 控制面 shell | 可以对标 | **强烈建议借** |
| doctor / inventory / logs / status | 可以对标 | **强烈建议借** |
| agent-facing docs | 可以对标 | **建议借** |

### 2.2 证据来源说明

| 来源 | 结论 |
|---|---|
| 直接读取竞品源码 | 本次关于 `agent-browser` 的结论以源码文件为准 |
| 本地 `.codegraph/codegraph.db` | 只覆盖当前 OpenPage 的一部分文件，不覆盖参考项目本身 |
| codegraph 当前索引状态 | 对当前 OpenPage 个别文件存在索引滞后迹象，因此 **不能只信 codegraph，必须回到真实源码文件** |

换句话说，这次可以用 codegraph 辅助看 **OpenPage 当前落点**，但竞品判断仍以参考项目源码本身为准。

### 2.3 2026-05-31 本机真实状态（OpenPage 当前机器）

下面这张表不是竞品结论，而是你这台机器上 **当前 OpenPage 工作树的真实运行态**。后续是否继续借鉴、接多少，应该以这张表为前提。

| 项目 | 当前真实状态 | 证据 / 涉及文件 |
|---|---|---|
| 活跃协议 | **TCP daemon 是唯一活跃协议** | `rust/target/debug/openpage --help`、`rust/target/debug/openpage serve --help` |
| 废弃入口 | `serve --stdio`、`page get`、`page url`、`page title`、`page screenshot` 当前都继续被 runtime 拒绝 | `rust/target/debug/openpage serve --stdio`；`rust/target/debug/openpage page url` 等 |
| 当前 daemon inventory | `browser list` 当前返回 `healthy=18 / incompatible=0 / incomplete=0 / cleaned=0 / total=18` | `rust/target/debug/openpage browser list` |
| 当前默认 doctor | `doctor --quick` 当前返回 `pass=22 / warn=0 / fail=1 / info=1 / total=24` | `rust/target/debug/openpage doctor --quick` |
| 当前唯一 fail | `browser.executable`，因为当前 dirty worktree 的 `rust/configs.ini` 里是 `browser_path=/tmp/dp-browser` | `rust/configs.ini`、`rust/target/debug/openpage doctor --quick` |
| 当前 machine-local workaround | `OPENPAGE_BROWSER_PATH=/Applications/Google Chrome.app/Contents/MacOS/Google Chrome` 可把 quick doctor 转成 `pass=23 / fail=0 / total=24` | `OPENPAGE_BROWSER_PATH=... rust/target/debug/openpage doctor --quick` |
| 当前已完成的一小步 | `doctor` 的 browser checks 现在会直接暴露 `browser_path`；在可解析场景下暴露 `resolved_path`；在缺失别名但本机可推断场景下暴露 `suggested_path` | `rust/src/cli/doctor.rs`、`cargo run ... doctor --quick`、临时 `dp_configs.ini(browser_path=chrome)` smoke |
| CDP / element / interaction 边界 | 当前没有证据表明需要借竞品 runtime 内核；你自己的实现仍应继续作为真相源 | `rust/src/browser.rs`、`rust/src/element.rs`、`rust/src/page.rs`、`rust/src/webpage.rs` |
| 当前最适合继续借的点 | **shell/control-plane 的错误上下文保真** 仍值得继续做 | `rust/src/cli/protocol.rs`、`rust/src/cli/oneshot.rs` |
| 当前不适合动的点 | 不应在这一步碰竞品 `cli/src/native/*` 或你自己的 runtime 内核 | `参考项目/agent-browser-main/cli/src/native/*` |

### 2.4 当前最值得继续接入的一小步

如果只选一个下一步，我建议继续做这件事：

| 候选点 | 原因 | 竞品参考 | OpenPage 落点 |
|---|---|---|---|
| daemon 结构化错误上下文保真 | 现在 `browser list/status/logs/doctor` 已经有 `session/state/reasons/fix`，但 direct CLI error 仍有一部分依赖 message 重建上下文 | `参考项目/agent-browser-main/cli/src/main.rs`、`cli/src/connection.rs` 的顶层错误纪律 | `rust/src/cli/protocol.rs`、`rust/src/cli/oneshot.rs` |

这一步仍然只动 **外壳层**，不会碰你的 browser/CDP/locator/interaction 真相源。

---

## 3. 速查总表：哪些值得学，哪些可以直接 copy

| 优先级 | 竞品文件 | 借鉴价值 | 是否适合直接 copy 后微调 | OpenPage 落点 | 结论 |
|---|---|---|---|---|---|
| P0 | `参考项目/agent-browser-main/cli/src/connection.rs` | daemon sidecar、inventory、stale cleanup、version guard、request retry | **高** | `rust/src/cli/connection.rs` | **第一优先级** |
| P0 | `参考项目/agent-browser-main/cli/src/doctor/mod.rs` | `Check` / `Summary` / `fix` / quick-vs-full 模型 | **高** | `rust/src/cli/doctor.rs` | **第一优先级** |
| P0 | `参考项目/agent-browser-main/cli/src/doctor/launch.rs` | launch smoke、Drop cleanup guard | **高** | `rust/src/cli/doctor.rs` | **第一优先级** |
| P1 | `参考项目/agent-browser-main/cli/src/output.rs` | boundary、truncate、origin-aware 输出治理 | **中高** | `rust/src/cli/protocol.rs` | **适合按函数借** |
| P1 | `参考项目/agent-browser-main/cli/src/main.rs` | 顶层 JSON error shell、统一输出出口 | **中** | `rust/src/cli/mod.rs`、`rust/src/cli/oneshot.rs` | **只借外壳** |
| P1 | `参考项目/agent-browser-main/cli/src/commands.rs` | `ParseError` 分层、cookie import 小工具 | **低中** | `rust/src/cli/args.rs` 或独立 util | **只挑小函数** |
| P1 | `参考项目/agent-browser-main/skill-data/core/*` | agent 文档组织、snapshot-ref 工作流 | **高** | `skills/openpage-test/*` | **很值得借** |
| P3 | `参考项目/agent-browser-main/cli/src/native/*` | 浏览器/CDP/element/runtime 内核 | **禁止** | 无 | **不要碰** |
| P3 | `参考项目/agent-browser-main/cli/src/chat.rs` | chat REPL | 低 | 无 | **当前不该做** |
| P3 | `参考项目/agent-browser-main/packages/dashboard/*` | dashboard UI | 低 | 无 | **与你当前主线无关** |

---

## 4. 最值得你直接借的部分

## 4.1 `cli/src/connection.rs`

这是整个竞品里最适合你直接 copy 再微调的文件。

### 为什么它最适合借

| 原因 | 说明 |
|---|---|
| 只碰控制面 | 不会污染你自己的 browser/CDP/element/runtime 真相源 |
| 与 TCP-only 完全同向 | 你只需要把 transport 固定成 TCP，不需要保留它的 Unix socket 分支 |
| 直接提高稳定性 | sidecar、ready poll、retry、version guard 都是 daemon CLI 的高价值能力 |
| 直接提高可诊断性 | inventory / logs / status / fix guidance 能共用同一套 session 真相 |

### 可以直接抄的函数 / 结构

| 竞品函数或结构 | 竞品文件 | 借鉴点 | OpenPage 当前对应文件 |
|---|---|---|---|
| `cleanup_stale_files()` | `cli/src/connection.rs` | sidecar 清理骨架 | `rust/src/cli/connection.rs` |
| `is_pid_alive()` | `cli/src/connection.rs` | 跨平台进程存活检查 | `rust/src/cli/connection.rs` |
| `ActiveSession` | `cli/src/connection.rs` | healthy session 模型 | `rust/src/cli/connection.rs` |
| `CleanReason` / `CleanedSession` | `cli/src/connection.rs` | cleaned/stale 分类 | `rust/src/cli/connection.rs` |
| `DaemonInventory` | `cli/src/connection.rs` | inventory 顶层模型 | `rust/src/cli/connection.rs` |
| `walk_daemons()` | `cli/src/connection.rs` | inventory 扫描主循环 | `rust/src/cli/connection.rs` |
| `daemon_version_matches()` | `cli/src/connection.rs` | 版本兼容检查 | `rust/src/cli/connection.rs` |
| `kill_stale_daemon()` | `cli/src/connection.rs` | 清理旧 daemon | `rust/src/cli/connection.rs` |
| `ensure_daemon()` | `cli/src/connection.rs` | 自动拉起和 ready 等待骨架 | `rust/src/cli/connection.rs` |
| `send_command_once()` | `cli/src/connection.rs` | 单次发送壳 | `rust/src/cli/connection.rs` |
| `is_transient_error()` | `cli/src/connection.rs` | retry 分类 | `rust/src/cli/connection.rs` |
| `send_command()` | `cli/src/connection.rs` | 带 retry 的统一出口 | `rust/src/cli/connection.rs` |

### copy 过来时必须删改的部分

| 竞品原样内容 | 你这里必须怎么改 | 原因 |
|---|---|---|
| `Connection::Unix` + `Connection::Tcp` 双栈 | **只保留 TCP** | 你已经定了唯一协议 |
| `get_socket_dir()` 及 `.sock` 逻辑 | 改成 `OPENPAGE_HOME/daemon` 目录与 `.port/.pid/.version` sidecar | OpenPage 的目录布局不同 |
| `Request` / `Response` 结构 | **不要抄** | OpenPage 已经有自己的协议真相 |
| `send_command()` 里的 `action` 风格 payload | 改成 OpenPage 自己的 `op/params` 风格 | 避免引入第二套协议语义 |
| 针对 Unix socket 的分支清理 | 全部删掉 | 避免把复杂度抄回来 |

### 对应到 OpenPage，最相关的当前文件

| OpenPage 文件 | 当前已承接内容 |
|---|---|
| `rust/src/cli/connection.rs` | `is_pid_alive`、`daemon_version_matches`、`kill_stale_daemon`、`existing_daemon_action*`、`send_request_once`、`send_request_with_retry` |
| `rust/src/cli/oneshot.rs` | `browser list`、`browser status`、`browser logs` 等消费 connection 层输出 |

### 判断

> 如果你后面真要“复制竞品代码再微调”，**最先动 `cli/src/connection.rs`**。

---

## 4.2 `cli/src/doctor/mod.rs` + `cli/src/doctor/launch.rs`

这一层同样非常适合借，而且借完马上能提升 OpenPage 的可维护性。

### 值得借的核心不是文案，是模型

| 模型 | 借鉴价值 |
|---|---|
| `Status` | 统一 `pass/warn/fail/info` 心智 |
| `Check` | 稳定 `id/category/status/message/fix` 结构 |
| `Summary` | 机器友好的统计摘要 |
| quick / full 分层 | 日常 smoke 与深度排障分离 |
| launch smoke | 用真实 browser launch 证明环境可用 |
| Drop guard | 保证 doctor 失败时也能清 scratch session |

### 可以直接抄的函数 / 结构

| 竞品函数或结构 | 竞品文件 | 借鉴点 | OpenPage 当前对应文件 |
|---|---|---|---|
| `DoctorOptions` | `cli/src/doctor/mod.rs` | doctor 参数组织方式 | `rust/src/cli/doctor.rs` |
| `Status` | `cli/src/doctor/mod.rs` | 状态枚举 | `rust/src/cli/doctor.rs` |
| `Check` | `cli/src/doctor/mod.rs` | 结构化检查项 | `rust/src/cli/doctor.rs` |
| `Summary` | `cli/src/doctor/mod.rs` | 汇总结构 | `rust/src/cli/doctor.rs` |
| `summarize()` | `cli/src/doctor/mod.rs` | 统计逻辑 | `rust/src/cli/doctor.rs` |
| `print_json()` | `cli/src/doctor/mod.rs` | doctor JSON 输出壳 | `rust/src/cli/doctor.rs` |
| `print_text()` | `cli/src/doctor/mod.rs` | 如后续需要 text 模式可参考 | `rust/src/cli/doctor.rs` |
| `LaunchGuard` | `cli/src/doctor/launch.rs` | launch smoke 清理保护 | `rust/src/cli/doctor.rs` |
| `send_json()` | `cli/src/doctor/launch.rs` | doctor 内部最小 RPC 壳 | `rust/src/cli/doctor.rs` |

### 当前 OpenPage 里已经有的对应落点

| OpenPage 文件 | 当前已承接内容 |
|---|---|
| `rust/src/cli/doctor.rs` | `Check`、`Summary`、`daemon_checks`、`browser_checks`、`BrowserLaunchGuard`、`TempDirGuard` |
| `rust/src/cli/connection.rs` | doctor 依赖的 inventory / fix / state / reasons 真相 |

### 判断

> `doctor` 对 TCP daemon 工具不是装饰品，而是核心控制面。这个层非常值得继续对标竞品。

---

## 4.3 `cli/src/output.rs`

这个文件不适合整文件搬，但非常适合按函数借思路。

### 最值得借的点

| 竞品函数 | 借鉴价值 | OpenPage 当前对应文件 |
|---|---|---|
| `get_boundary_nonce()` | 进程稳定 boundary nonce | `rust/src/cli/protocol.rs` |
| `truncate_if_needed()` | 长文本截断策略 | `rust/src/cli/protocol.rs` |
| `print_with_boundaries()` | content boundary 包裹 | `rust/src/cli/protocol.rs` |
| `print_response_with_opts()` | 统一输出出口心智 | `rust/src/cli/protocol.rs` |

### 借法建议

| 建议 | 说明 |
|---|---|
| 借 boundary contract | agent 消费收益最高 |
| 借 truncate 策略 | 避免超长输出污染 CLI / agent |
| 借 origin-aware payload 习惯 | 对 snapshot/html/text/logs 都有价值 |
| 不借 help 文本 | 竞品的命令面和你不同 |
| 不引入第二套 response schema | 继续保留 OpenPage 自己的协议结构 |

### 当前 OpenPage 对应落点

| OpenPage 文件 | 当前已承接内容 |
|---|---|
| `rust/src/cli/protocol.rs` | `boundary_nonce`、`truncate_string`、`wrap_content`、`apply_output_filters`、`format_output_json` |

### 判断

> `output.rs` 适合按小函数借，不适合整文件照搬。

---

## 4.4 `cli/src/main.rs`

这一层只建议借 **顶层 JSON error shell**，不要借整个命令分发。

### 适合借的函数

| 竞品函数 | 借鉴价值 | OpenPage 当前对应文件 |
|---|---|---|
| `serialize_json_value()` | 顶层统一序列化出口 | `rust/src/cli/mod.rs`、`rust/src/cli/protocol.rs` |
| `print_json_value()` | 顶层统一打印壳 | `rust/src/cli/mod.rs`、`rust/src/cli/oneshot.rs` |
| `print_json_error()` | 顶层错误出口 | `rust/src/cli/protocol.rs` |
| `print_json_error_with_type()` | 稳定错误类型 | `rust/src/cli/protocol.rs` |

### 这层真正值得学的不是函数名，而是契约纪律

| 契约纪律 | 价值 |
|---|---|
| 顶层错误也给稳定 `error.kind` | 调用方不必扫自由文本 |
| 已知 session/control-plane 失败带 `fix` | 提高可恢复性 |
| 已知 session/control-plane 失败带 `state/reasons` | direct error 与 `status/list/logs/doctor` 共享同一状态语言 |
| session 名独立给字段 | 不必从 message 里反切 session |
| 空场景也尽量保持对象 shape 稳定 | 减少自动化分支 |

### 当前 OpenPage 对应落点

| OpenPage 文件 | 当前已承接内容 |
|---|---|
| `rust/src/cli/protocol.rs` | `simple_error*`、`simple_openpage_error`、`response_openpage_error`、`openpage_error_context` |
| `rust/src/cli/mod.rs` | `print_json_value`、`clap_error_payload`、`print_clap_error` |
| `rust/src/cli/oneshot.rs` | `response_result`、`print_json` |

### 判断

> 这里借“错误壳思路”即可，不要动命令框架本身。

---

## 4.5 `cli/src/commands.rs`

这不是大块 copy 候选，只是一些零散工具可以取样。

| 竞品函数或结构 | 是否值得借 | 说明 |
|---|---|---|
| `ParseError` | 可部分借 | 适合借错误分层思路 |
| `parse_curl_cookies()` | 视需求 | 如果 OpenPage 以后增强 cookie import，可直接参考 |
| `shell_words_split()` | 视需求 | 如果以后扩 batch / REPL，可参考 |
| `gen_id()` | 价值低 | 可有可无 |
| `parse_command()` 主体 | **不要借** | 与竞品命令体系耦合太深 |

---

## 4.6 `skill-data/core/*`

这个部分代码不多，但对 agent 体验非常值钱。

### 值得借的是组织方式

| 竞品文档 | 用处 | OpenPage 对应落点 |
|---|---|---|
| `skill-data/core/SKILL.md` | agent 入口说明 | `skills/openpage-test/SKILL.md` |
| `skill-data/core/references/*.md` | 分主题知识拆分 | `skills/openpage-test/references/*.md` |
| snapshot-and-ref workflow | 让 agent 形成稳定主循环 | `snapshot-refs.md`、`cli-smoke.md` |
| trust boundary 文档 | 让 agent 知道哪些字段可信 | `trust-boundaries.md` |
| session 管理文档 | 让 agent 知道哪些命令会建 session | `session-management.md` |

### 判断

> 如果你的目标是让后续 agent 少走弯路，这部分甚至比某些代码 borrow point 更值钱。

---

## 5. 明确不要借的部分

| 竞品文件或目录 | 不要借的原因 | 风险 |
|---|---|---|
| `参考项目/agent-browser-main/cli/src/native/actions.rs` | 动作执行内核 | 会污染 OpenPage 自己的交互真相源 |
| `参考项目/agent-browser-main/cli/src/native/browser.rs` | 浏览器封装内核 | 会污染你自己的 browser/CDP 封装 |
| `参考项目/agent-browser-main/cli/src/native/element.rs` | 元素模型内核 | 会污染 locator/element contract |
| `参考项目/agent-browser-main/cli/src/native/interaction.rs` | 交互内核 | 会污染行为语义 |
| `参考项目/agent-browser-main/cli/src/native/snapshot.rs` | snapshot 抓取内核 | 会污染 OpenPage 自己的 snapshot 真相 |
| `参考项目/agent-browser-main/cli/src/native/network.rs` | network runtime | 会带入不必要 runtime 假设 |
| `参考项目/agent-browser-main/cli/src/native/state.rs` | runtime state 深耦合 | 难以局部迁移 |
| `参考项目/agent-browser-main/cli/src/native/providers.rs` | provider/cloud 适配 | 当前主线不需要 |
| `参考项目/agent-browser-main/cli/src/chat.rs` | chat REPL | 与当前 TCP 主线无关 |
| `参考项目/agent-browser-main/packages/dashboard/*` | dashboard UI | 与当前主线关系弱 |

---

## 6. 当前 OpenPage 里，哪些文件最适合承接这些借鉴

| OpenPage 文件 | 角色 | 最适合承接的竞品能力 |
|---|---|---|
| `rust/src/cli/connection.rs` | daemon 控制面真相源 | sidecar、inventory、version guard、retry、ready poll |
| `rust/src/cli/doctor.rs` | 环境与 daemon 诊断面 | `Check`/`Summary`/`fix`/launch smoke/cleanup guard |
| `rust/src/cli/protocol.rs` | 输出与错误壳 | boundary、truncate、origin-aware output、top-level error shell |
| `rust/src/cli/mod.rs` | 顶层入口 | clap error 到 JSON error 的统一出口 |
| `rust/src/cli/oneshot.rs` | CLI 命令壳 | `browser list/status/logs` 等控制面输出消费 |
| `skills/openpage-test/*` | agent-facing docs | skill-data/core 风格的组织方式 |

---

## 7. 如果后面真的要 copy，建议按这个顺序

| 顺序 | 建议动作 | 目标 | 涉及文件 |
|---|---|---|---|
| 1 | 继续从 `connection.rs` 借控制流 | 进一步收紧 daemon 生命周期、session 状态和 fix guidance | `参考项目/agent-browser-main/cli/src/connection.rs` → `rust/src/cli/connection.rs` |
| 2 | 继续从 `doctor` 借诊断模型 | 让 quick/full/checks/summary/fix 更稳定 | `cli/src/doctor/mod.rs`、`cli/src/doctor/launch.rs` → `rust/src/cli/doctor.rs` |
| 3 | 继续从 `output.rs` 借输出治理 | 稳定 snapshot/html/text/logs 的 agent 消费体验 | `cli/src/output.rs` → `rust/src/cli/protocol.rs` |
| 4 | 顶层 error shell 再统一 | 所有 direct CLI error 与 daemon error 说同一种机器语言 | `cli/src/main.rs` → `rust/src/cli/mod.rs`、`rust/src/cli/oneshot.rs`、`rust/src/cli/protocol.rs` |
| 5 | 文档继续按 `skill-data/core` 方式整理 | 降低后续 agent 误用率 | `skill-data/core/*` → `skills/openpage-test/*` |
| 6 | 最后才考虑小工具函数 | 只补缺，不大搬 | `cli/src/commands.rs` |

---

## 8. copy 时的硬约束

| 约束 | 必须怎么做 |
|---|---|
| 协议唯一性 | 只允许 TCP daemon，不能把 Unix socket / stdio 第二真相抄回来 |
| 协议结构 | 保留 OpenPage 自己的 `Request/Response`，不要抄竞品 `success/action/data` 结构 |
| 内核边界 | 不借 `native/*`、不借浏览器/CDP/locator/interaction 真相源 |
| 借鉴方式 | 优先抄“小函数 + 控制流骨架”，不要整层替换 |
| 输出契约 | 复用你当前 `error.kind`、`error.fix`、`error.state`、`error.reasons` 体系，不要并存两套 |
| 验证方式 | 每借一层都补定向测试，尤其保证旧协议入口不要回流 |
| 文档同步 | 代码借完后同步 `README` 和 `skills/openpage-test/*` |

---

## 9. 最终判断

如果你现在问：

> “这个竞品里，哪些部分我甚至可以直接 copy 过来，再做 TCP-only 微调？”

我给的排序是：

### 第一梯队：可以直接 copy 后微调

1. `参考项目/agent-browser-main/cli/src/connection.rs`
2. `参考项目/agent-browser-main/cli/src/doctor/mod.rs`
3. `参考项目/agent-browser-main/cli/src/doctor/launch.rs`
4. `参考项目/agent-browser-main/cli/src/output.rs` 里的小函数

### 第二梯队：只借思路，不建议大抄

1. `参考项目/agent-browser-main/cli/src/main.rs`
2. `参考项目/agent-browser-main/cli/src/commands.rs`
3. `参考项目/agent-browser-main/skill-data/core/*`

### 第三梯队：明确不要借

1. `参考项目/agent-browser-main/cli/src/native/*`
2. `参考项目/agent-browser-main/cli/src/chat.rs`
3. `参考项目/agent-browser-main/packages/dashboard/*`

---

## 10. 最务实的执行策略

站在你现在这个阶段，最合理的借鉴姿势不是“全面参考 agent-browser”，而是：

> **把它当成一个 TCP daemon CLI 的控制面与外壳层参考实现。**

具体就是：

- 借它的 **connection / inventory / retry / stale cleanup**
- 借它的 **doctor / launch smoke / fix guidance**
- 借它的 **output boundary / truncate / top-level error shell**
- 借它的 **skill-data 风格文档组织**
- 不借它的 **browser runtime / CDP / locator / interaction / snapshot 内核**

这条边界是对的，而且值得继续坚持。
