# 竞品文档：考虑借鉴的部分 v1

## 0. 调研边界与依据

这份文档只回答一件事：

**在你已经确定 OpenPage 走 TCP daemon 模式的前提下，`agent-browser` 有哪些“外壳层设计”值得借，哪些代码可以直接 copy 再微调，哪些绝对不要搬。**

### 本次明确不做的事

- 不建议引入竞品的 CDP transport
- 不建议引入竞品的 element / interaction 内核
- 不建议引入竞品的 snapshot 底层生成算法
- 不建议引入竞品的 provider / chat / dashboard 作为当前阶段主线

### 本次核对的本地依据

#### 竞品代码
- `参考项目/agent-browser-main/cli/src/connection.rs`
- `参考项目/agent-browser-main/cli/src/doctor/*.rs`
- `参考项目/agent-browser-main/cli/src/output.rs`
- `参考项目/agent-browser-main/cli/src/main.rs`
- `参考项目/agent-browser-main/skill-data/core/*`
- `参考项目/agent-browser-main/README.md`

#### OpenPage 当前代码
- `rust/src/cli/connection.rs`
- `rust/src/cli/serve.rs`
- `rust/src/cli/oneshot.rs`
- `rust/src/cli/protocol.rs`
- `rust/src/cli/doctor.rs`
- `rust/src/cli/args.rs`
- `README.md`
- `skills/openpage-test/*`

---

## 1. 先给结论

### 1.1 对 OpenPage 最有价值的借鉴方向

`agent-browser` 最值得借的，不是浏览器内核，而是这四层：

1. **daemon 外壳层**
   - sidecar
   - session inventory
   - stale cleanup
   - 版本错配重启
   - 启动失败日志回传
2. **doctor 诊断层**
   - checks
   - summary
   - fix hint
   - launch smoke
3. **agent 输出层**
   - boundary
   - truncate
   - trust boundary 文档
   - AI-first snapshot contract
4. **CLI 编排层**
   - batch
   - 更清晰的帮助面
   - 面向 agent 的技能文档

### 1.2 对 OpenPage 当前方向的判断

你现在选 **TCP** 是对的。这个方向和竞品最值得借的部分高度一致。

因为竞品真正成熟的地方，不是“如何操作 DOM”，而是：

> 如何把浏览器能力包装成一个稳定、可诊断、适合 agent 消费的 daemon CLI。

这正好和你现在的主目标一致。

---

## 2. OpenPage 当前本地现状核对

下面不是“应该怎样”，而是**你当前本地代码已经是什么样**。

| 目标 | 当前本地状态 | 结论 | 主要证据文件 |
|---|---|---|---|
| 唯一稳定协议路径 | 活跃公开路径已经收敛到 TCP daemon | **基本达成**，但仍要继续清理历史文档噪音 | `rust/src/cli/serve.rs`, `rust/src/cli/oneshot.rs`, `README.md`, `skills/openpage-test/*` |
| 旧废弃执行路径 | `open_page()` / `load_session()` / `Browser::connect()` / `do_start_browser()` 已不在活跃 CLI 流程里 | **已从主路径移除** | `rust/src/cli/oneshot.rs`, 当前 repo 内检索结果 |
| 保留 OpenPage 自己的 CDP / element / interaction | 当前借鉴点集中在 `cli` 外壳层，没有把竞品 `native/*` 核心搬进来 | **方向正确** | `rust/src/cli/connection.rs`, `rust/src/cli/doctor.rs`, `rust/src/cli/protocol.rs`, `rust/src/cli/serve.rs` |
| daemon sidecar / inventory | 已有 `.port/.pid/.version/.log`，并有 inventory / stale cleanup / browser list / doctor 接线 | **第一版已接入** | `rust/src/cli/connection.rs`, `rust/src/cli/doctor.rs`, `rust/src/cli/serve.rs`, `rust/src/cli/oneshot.rs` |
| doctor 诊断体系 | 已有 `doctor` / `doctor --quick`，能检查环境、sidecar、浏览器 launch，并给出浏览器路径 hint | **最小可用版已接入** | `rust/src/cli/doctor.rs` |
| output boundary / truncate | 已接入 `OPENPAGE_CONTENT_BOUNDARIES` / `OPENPAGE_MAX_OUTPUT_CHARS` | **已接入，但 nonce 方案还可以继续抄强一点** | `rust/src/cli/protocol.rs`, `rust/src/cli/mod.rs`, `rust/src/cli/oneshot.rs` |
| batch | 已支持 `batch` / `--bail` / stdin JSON | **已接入** | `rust/src/cli/args.rs`, `rust/src/cli/oneshot.rs` |
| AI-first snapshot contract | `snapshot` 已返回 `snapshot` / `text` / `refs` / `origin` / `title` / `interactive_count` | **已接入第一版** | `rust/src/cli/serve.rs`, `README.md`, `skills/openpage-test/references/cli-smoke.md` |
| trust boundary / agent 文档 | 还没有竞品那样完整的 agent 安全文档体系 | **明显还欠一块** | `skills/openpage-test/*` 与竞品 `skill-data/core/references/*` 对比 |
| 当前机器浏览器配置 | 当前 `browser_path=chrome` 在本机不可解析，但本机存在可用 Chrome 绝对路径 | **需要人工修配置，不建议硬编码进仓库** | `rust/src/cli/doctor.rs`, 本机 `doctor --quick` 结果 |

### 当前机器已确认的具体情况

| 项目 | 当前值 / 现象 | 结论 |
|---|---|---|
| `OPENPAGE_HOME` | `/Users/yuuu/.openpage` | 正常 |
| healthy daemon sessions | `cli-more-states-2`, `cli-state-queries`, `human-flow` | sidecar/inventory 正在实际使用 |
| `rust/configs.ini` 中浏览器配置 | `browser_path=chrome` | 当前机器不可解析 |
| 本机实际可用浏览器路径 | `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome` | 可用于本机 smoke / 调试 |

### 对你的三个核心要求的判断

| 你的要求 | 当前判断 |
|---|---|
| 1. 唯一稳定 TCP 协议，不要再出现废弃方式 | **主路径已经对了**，后续重点是继续删历史误导面，不是重做协议设计 |
| 2. 元素定位 / 交互 / CDP 封装坚持用自己实现 | **当前执行也符合这个边界**，不要回头搬竞品 `cli/src/native/*` |
| 3. 非 CDP 设计尽量抄过来 | **已经借进来一部分了**，下一步是继续补强 doctor / nonce / trust-boundary 文档 |

---

## 3. 借鉴优先级总表（含 OpenPage 当前状态）

| 优先级 | 借鉴项 | 竞品文件 | OpenPage 当前状态 | 是否建议直接 copy | OpenPage 对应落点 |
|---|---|---|---|---|---|
| P0 | daemon sidecar + inventory + stale cleanup | `cli/src/connection.rs` | **已接入第一版** | **是**，结构和流程都很适合直接参考 | `rust/src/cli/connection.rs`, `rust/src/cli/serve.rs`, `rust/src/cli/doctor.rs` |
| P0 | doctor 检查框架 | `cli/src/doctor/mod.rs`, `cli/src/doctor/daemon.rs`, `cli/src/doctor/launch.rs` | **已接入最小版** | **是**，尤其是 check/fix/summary/LaunchGuard | `rust/src/cli/doctor.rs` |
| P0 | output boundary + truncate | `cli/src/output.rs` | **已接入，但还有可加强点** | **是**，尤其是 nonce 和输出包装细节 | `rust/src/cli/protocol.rs` |
| P1 | batch | `cli/src/main.rs`, `cli/src/commands.rs` | **已接入** | **是**，借产品语义和主循环骨架 | `rust/src/cli/args.rs`, `rust/src/cli/oneshot.rs` |
| P1 | snapshot + refs 的 agent contract | `README.md`, `skill-data/core/SKILL.md`, `references/snapshot-refs.md` | **已接入第一版** | **是**，但借契约和文档，不借底层实现 | `rust/src/cli/serve.rs`, `skills/openpage-test/*` |
| P1 | trust boundary / session 文档体系 | `skill-data/core/references/trust-boundaries.md`, `session-management.md` | **还缺** | **是**，适合直接参考文档结构 | `skills/openpage-test/*` 或新的 OpenPage skill 文档 |
| P2 | browser/config 诊断提示 | `cli/src/doctor/chrome.rs`, `cli/src/doctor/config.rs` | **已有第一版 hint** | **可以**，继续补 fix hint 即可 | `rust/src/cli/doctor.rs` |
| P3 | chat REPL / AI orchestration | `cli/src/chat.rs` | **未接入** | **不建议现在做** | 暂不进入主线 |
| P3 | dashboard / providers | `packages/dashboard/*`, `cli/src/native/providers.rs` | **未接入** | **不建议现在做** | 暂不进入主线 |

---

## 4. 最值得直接 copy 再微调的代码清单

这部分是你后面最可能直接抄的。

| 借鉴对象 | 竞品文件 | 为什么值得直接抄 | OpenPage 当前状态 | 微调建议 |
|---|---|---|---|---|
| `ActiveSession` / `CleanedSession` / `DaemonInventory` 这类模型 | `cli/src/connection.rs` | 数据结构边界很清楚 | 已有同类结构 | 保持 TCP-only，只保留 `.port/.pid/.version/.log` |
| `walk_daemons()` 扫描分类逻辑 | `cli/src/connection.rs` | sidecar 扫描、分类、清理逻辑成熟 | 已借入主思路 | 按 OpenPage sidecar 规则继续精简 |
| `cleanup_stale_files()` | `cli/src/connection.rs` | 非常契合 daemon 生命周期 | 已接入 | 保持当前最小 sidecar 集合 |
| version mismatch restart 策略 | `cli/src/connection.rs` | 能避免旧 daemon 和新 CLI 协议不匹配 | 已接入 | 保持现有策略，不要回退 |
| 启动失败回传 log 路径 / 内容 | `cli/src/connection.rs` | 对定位问题非常实用 | 已接入 | 后续只需继续润色错误文案 |
| `Check` / `Summary` / `with_fix()` 模型 | `cli/src/doctor/mod.rs` | structured doctor 输出非常清晰 | 还没完全抄到位 | 建议继续加 `fix` 字段，统一文本/JSON 输出 |
| `LaunchGuard` 模式 | `cli/src/doctor/launch.rs` | scratch launch + Drop 清理很干净 | 还可继续借 | 建议把 launch smoke 生命周期再收紧一点 |
| `truncate_if_needed()` | `cli/src/output.rs` | 简单、稳定、实用 | 已有同类逻辑 | 可以继续对齐提示文案 |
| CSPRNG nonce 生成 | `cli/src/output.rs` | 能防止页面内容伪造 boundary | **当前 OpenPage 还没借到位** | 建议直接把 `pid+timestamp` 改为 `getrandom` 风格 |
| boundary metadata 注入模式 | `cli/src/output.rs` | 对 agent 非常友好 | 已有第一版 | 继续保持 `_boundary.keys` / `nonce` 这种结构 |
| batch 主循环 | `cli/src/main.rs` | 子命令批执行骨架成熟 | 已接入第一版 | 保持你自己的 clap / command tree，不要硬搬 parser |
| stdin JSON batch 输入格式 | `cli/src/main.rs` | 对 agent 很友好 | 已接入 | 无需大改 |

### 这部分里，当前最值得你马上再抄一刀的只有两个点

1. **`protocol.rs` 的 boundary nonce 改成 CSPRNG**
2. **`doctor.rs` 的 check/fix/smoke 生命周期再向竞品靠拢**

这两个投入小，收益很实。

---

## 5. 只建议借鉴思路，不建议直接拷贝的部分

| 模块 | 竞品文件 | 为什么不建议直接抄 | 你的边界 |
|---|---|---|---|
| 浏览器内核 / 动作执行层 | `cli/src/native/browser.rs`, `cli/src/native/actions.rs`, `cli/src/native/daemon.rs` | 强绑定它自己的 CDP 模型和 action dispatch | 继续用 OpenPage 自己的实现 |
| element / interaction | `cli/src/native/element.rs`, `cli/src/native/interaction.rs` | 和它的 DOM/ref/状态模型耦合很深 | 不搬 |
| snapshot 底层生成逻辑 | `cli/src/native/snapshot.rs` | 契约可借，算法别抄 | 只借 contract |
| network / providers | `cli/src/native/network.rs`, `cli/src/native/providers.rs` | 功能面过宽，会把主线带偏 | 暂不引入 |
| chat 自然语言控制层 | `cli/src/chat.rs` | 依赖它自己的 AI orchestration | 暂不引入 |
| dashboard | `packages/dashboard/*` | 当前对 TCP daemon 主路径没帮助 | 暂不引入 |

### 一句话边界

**可以抄“外壳层”，不要抄“内核层”。**

---

## 6. 对应到 OpenPage，各文件现在应该怎么理解

### 6.1 已经成为借鉴承接点的本地文件

| OpenPage 文件 | 当前职责 | 对应竞品借鉴方向 |
|---|---|---|
| `rust/src/cli/connection.rs` | daemon sidecar、inventory、auto-start、retry、version handling | `cli/src/connection.rs` |
| `rust/src/cli/serve.rs` | TCP daemon request handling、snapshot contract、session runtime state | `cli/src/native/daemon.rs` 的外壳思路 + README/skills 的 agent contract |
| `rust/src/cli/doctor.rs` | 环境检查、sidecar 检查、launch smoke、浏览器路径 hint | `cli/src/doctor/*` |
| `rust/src/cli/protocol.rs` | NDJSON contract、boundary、truncate、JSON output filter | `cli/src/output.rs` |
| `rust/src/cli/oneshot.rs` | CLI wrapper、batch 主循环、统一 daemon RPC 出口 | `cli/src/main.rs`, `cli/src/commands.rs` |
| `rust/src/cli/args.rs` | 用户面命令和参数定义 | `cli/src/flags.rs`, `cli/src/commands.rs` |
| `skills/openpage-test/*` | OpenPage 的 agent 使用说明和 smoke 操作面 | `skill-data/core/*` |

### 6.2 你现在不该动来“借鉴”的本地文件

| OpenPage 文件方向 | 原因 |
|---|---|
| `rust/src/browser.rs` / `rust/src/page.rs` / 元素相关内部实现 | 这是你自己的真相源，不该因为竞品外壳设计去污染 |
| 任何内部 locator / element state / interaction 细节 | 当前需求不是替换内核 |
| Python API 表层兼容逻辑 | 本轮目标是 CLI / daemon 外壳，不是 Python 用户态 |

---

## 7. 按你当前本地情况，下一步最值得接入的顺序

### 第一批：马上值得做

1. **把 `rust/src/cli/protocol.rs` 的 boundary nonce 改成 CSPRNG**
   - 当前仍是 `pid + timestamp`
   - 竞品做法更严谨
   - 改动小，收益直接

2. **继续把 `rust/src/cli/doctor.rs` 向 `Check + fix + launch guard` 结构收敛**
   - 你现在已有 `doctor --quick`
   - 下一步是把 structured fix hint 也收得更完整
   - 不一定要马上做 destructive `--fix`，但数据模型可以先对齐

3. **把 trust-boundary / snapshot-refs / session-management 文档补起来**
   - 这会直接提升后续 agent 使用成功率
   - 这部分借文档比借代码更值

### 第二批：继续巩固 TCP-only 心智

4. **继续清历史文档和帮助面里会误导人的旧协议叙述**
   - 当前主路径已经对了
   - 后续重点是“不要让旧表述重新污染未来会话”

5. **保持 sidecar / inventory / browser list / doctor 这四个面的一致性**
   - 不要一个面显示 healthy session
   - 另一个面又显示另一套状态

### 第三批：先不做

6. chat orchestration
7. dashboard
8. providers / cloud 扩展

---

## 8. 如果你后面真的要直接 copy，优先抄哪几个文件

### 第一优先级
- `参考项目/agent-browser-main/cli/src/connection.rs`
- `参考项目/agent-browser-main/cli/src/doctor/mod.rs`
- `参考项目/agent-browser-main/cli/src/doctor/launch.rs`
- `参考项目/agent-browser-main/cli/src/output.rs`

### 第二优先级
- `参考项目/agent-browser-main/cli/src/main.rs`
- `参考项目/agent-browser-main/cli/src/commands.rs`
- `参考项目/agent-browser-main/skill-data/core/SKILL.md`
- `参考项目/agent-browser-main/skill-data/core/references/snapshot-refs.md`
- `参考项目/agent-browser-main/skill-data/core/references/session-management.md`
- `参考项目/agent-browser-main/skill-data/core/references/trust-boundaries.md`

### 明确不要碰
- `参考项目/agent-browser-main/cli/src/native/*`
- `参考项目/agent-browser-main/cli/src/chat.rs`
- `参考项目/agent-browser-main/packages/dashboard/*`

---

## 9. 许可证提醒

竞品当前仓库是 **Apache-2.0**。

如果你后面是“借鉴思路”，问题不大。
如果你后面准备直接复制成块代码，最好提前确认两件事：

1. OpenPage 当前仓库许可证是否兼容
2. 是否需要保留原始版权与 notice

---

## 10. 最后的判断

对 OpenPage 来说，`agent-browser` 最值得学的不是“浏览器驱动本身”，而是：

**如何把浏览器能力包装成一个跨平台、可诊断、适合 agent 调用、对 prompt injection 更稳的 TCP daemon CLI。**

这和你现在的主方向是一致的。
