# agent-browser 竞品分析：OpenPage 在 TCP 模式下可考虑借鉴的部分 v1

## 1. 前提与结论

### 前提
- 你的通信模式已经确定为 **TCP daemon**，目标是 **全平台一致**。
- 这个前提下，`agent-browser` 最值得学的，不是它的 CDP / 浏览器 / 元素交互内核，而是它的 **CLI 外壳层设计**。

### 结论
- **最值得借鉴**：daemon 生命周期管理、sidecar 文件治理、批处理、doctor 诊断、LLM 友好的输出治理。
- **可以直接 copy 再微调的部分**：主要集中在 `cli/src/connection.rs`、`cli/src/main.rs`、`cli/src/doctor/*`、`cli/src/output.rs`、`cli/src/upgrade.rs`、`cli/src/install.rs`。
- **不建议直接抄**：`cli/src/native/*` 里的 browser / snapshot / element / interaction / actions 主体实现。它们和竞品自己的 CDP、AX tree、ref map、状态模型耦合太深。

一句话判断：

> **抄外壳，不抄内核。**

---

## 2. 借鉴优先级总表

| 优先级 | 模块 | 借鉴价值 | 是否适合 TCP-only | 建议动作 | 竞品文件 | OpenPage 落点 |
|---|---|---:|---:|---|---|---|
| P0 | daemon sidecar 治理 | 很高 | 是 | 可直接 copy 思路和部分代码 | `cli/src/connection.rs:93,131,242,523,579,779,816,834` | `rust/src/cli/connection.rs:54,71,188,257,413,478,502,528` |
| P0 | daemon inventory / stale cleanup / close-all | 很高 | 是 | 可直接 copy，删掉 Unix socket 分支 | `cli/src/connection.rs:216,242` + `cli/src/main.rs:408` | `rust/src/cli/connection.rs:115,136,148,156,236,253,257` + `rust/src/cli/mod.rs` |
| P0 | batch 批处理 | 很高 | 是 | 可直接 copy 交互模型 | `cli/src/main.rs:1216,1305` + `cli/src/commands.rs` | `rust/src/cli/oneshot.rs:708` + `rust/src/cli/args.rs:176,688` |
| P0 | 输出治理（boundary / truncate） | 很高 | 是 | 可直接 copy 思路，按 OpenPage 协议改字段 | `cli/src/output.rs:1,133` | `rust/src/cli/protocol.rs:1,183` |
| P1 | doctor 诊断框架 | 高 | 是 | 强烈建议继续借鉴 | `cli/src/doctor/mod.rs:95` + `cli/src/doctor/chrome.rs` + `cli/src/doctor/environment.rs` + `cli/src/doctor/daemon.rs` | `rust/src/cli/doctor.rs:65` |
| P1 | 版本不匹配自动重启 daemon | 高 | 是 | 可直接 copy | `cli/src/connection.rs:523,579` | `rust/src/cli/connection.rs:188,413` |
| P1 | 重试与瞬时错误分类 | 高 | 是 | 可直接 copy | `cli/src/connection.rs:779,816,834` | `rust/src/cli/connection.rs:478,502,528` |
| P2 | install / upgrade 体验 | 中 | 是 | 参考后按分发方式改 | `cli/src/install.rs:400` + `cli/src/upgrade.rs:46,187` + `scripts/postinstall.js` | 当前 OpenPage 尚无等价完整层 |
| P2 | skills 打包与发现 | 中 | 是 | 只借结构，不急着抄 | `cli/src/skills.rs` + `skill-data/` + `skills/` | `skills/`、README、后续 agent 文档体系 |
| P2 | snapshot 输出契约 | 中高 | 是，但只借“返回格式” | 只借 contract，不抄实现 | `cli/src/native/actions.rs:2466` | `rust/src/webpage.rs` / `rust/src/page.rs` / `rust/src/cli` |
| P3 | runtime stream / dashboard 元数据 | 中低 | 是 | 可暂缓 | `cli/src/native/daemon.rs` + `cli/src/native/actions.rs` | 取决于你后续是否要实时可视化 |

---

## 2.1 当前 OpenPage 对照

基于当前仓库代码，下面这些借鉴项已经**部分落地**：

| 借鉴项 | OpenPage 当前状态 | 当前文件 |
|---|---|---|
| TCP sidecar 基础设施 | 已有 | `rust/src/cli/connection.rs` |
| daemon inventory 数据结构 | 已有第一版 | `rust/src/cli/connection.rs` |
| output boundary / truncate | 已有第一版 | `rust/src/cli/protocol.rs` |
| batch | 已接入第一版 | `rust/src/cli/oneshot.rs` + `rust/src/cli/args.rs` |
| doctor | 已有第一版，但仍偏单文件 | `rust/src/cli/doctor.rs` |

所以后面如果继续借竞品，重点不是“从零照抄”，而是：

- 补齐 `doctor` 对 `daemon inventory / incomplete / cleaned` 的消费
- 继续收敛 daemon 外壳治理细节
- 在不碰 OpenPage 浏览器内核的前提下增强 CLI 体验

---

## 3. 最值得学的点：为什么值钱

## 3.1 daemon sidecar 这一层非常值得学

`agent-browser` 的核心优势之一，不是“它能控浏览器”，而是它把 **长生命周期 daemon 的运维问题** 想得比较完整。

它做了这些事：

| 点 | 竞品做法 | 为什么值得借鉴 |
|---|---|---|
| sidecar 文件 | 每个 session 维护 `.pid` / `.version` / `.sock` 或 `.port` / `.stream` | CLI 不需要额外状态中心，纯文件即可恢复现场 |
| stale cleanup | 启动前和巡检时清理无效 sidecar | 避免“端口文件还在但进程已死”这种脏状态 |
| inventory | 扫描 daemon 目录，列出活跃 session 和被清理的脏 session | 非常适合 `browser list` / `doctor` / `close --all` |
| version match | CLI 版本和 daemon 版本不一致时自动重启 daemon | 避免升级后二进制和后台进程协议不一致 |
| retry | 对连接拒绝、broken pipe、空响应做瞬时重试 | CLI 在进程启动边界会稳很多 |

### 可以直接借的函数级目标

| 竞品函数 | 结论 | 迁移建议 |
|---|---|---|
| `walk_daemons()` | 很适合直接借 | 改成只扫描 `.port/.pid/.version`，去掉 `.sock` 分支 |
| `cleanup_stale_files()` | 很适合直接借 | OpenPage 保留 `cleanup_sidecars()` 思路即可 |
| `daemon_version_matches()` | 很适合直接借 | 直接保留逻辑 |
| `kill_stale_daemon()` | 很适合直接借 | 按你现有 kill 方案合并 |
| `send_command()` / `send_command_once()` | 很适合直接借 | OpenPage 里已经是 `send_request()` 这一路，继续沿用即可 |
| `is_transient_error()` | 很适合直接借 | 这个函数性价比很高，收益立刻可见 |

### 对 OpenPage 的落点

- 竞品文件：
  - `参考项目/agent-browser-main/cli/src/connection.rs`
  - `参考项目/agent-browser-main/cli/src/main.rs`
- 你当前项目对应：
  - `rust/src/cli/connection.rs`
  - `rust/src/cli/mod.rs`
  - `rust/src/cli/doctor.rs`

### 当前判断

你现在已经选了 **TCP-only**，所以：

- **该借的**：inventory、stale cleanup、version check、retry、close-all。
- **不用借的**：`UnixStream` / `.sock` / Unix socket path 长度校验。

---

## 3.2 batch 批处理非常适合直接借

这个点对 agent CLI 很实用，原因不是“功能复杂”，而是它解决了 **多条命令串行时的进程开销和中间态表达**。

竞品做法：

| 能力 | 竞品实现 |
|---|---|
| 参数模式 | `agent-browser batch "open ..." "snapshot" "click @e1"` |
| stdin JSON 模式 | `[[\"open\", \"https://...\"], [\"snapshot\"]]` |
| `--bail` | 失败即停 |
| 嵌套限制 | 禁止嵌套 `batch`，避免递归和语义混乱 |

### 为什么适合你

- 和 TCP daemon 非常搭。
- 完全不依赖竞品内部浏览器内核。
- 只是 CLI orchestration 层。
- 迁移成本低，收益高。

### 文件映射

| 竞品文件 | 可借内容 | OpenPage 对应文件 |
|---|---|---|
| `cli/src/main.rs:1216,1305` | `run_batch()` 的整体流程 | `rust/src/cli/oneshot.rs:708` |
| `cli/src/commands.rs` | 字符串命令切词、命令解析错误结构 | `rust/src/cli/args.rs` / `rust/src/cli/oneshot.rs` |

### 当前判断

这个模块属于 **可直接 copy 交互模型，再按你现有 clap 结构微调** 的类型。

---

## 3.3 doctor 诊断框架值得继续借

竞品的 doctor 不是“功能炫”，而是 **工程上很实用**：

- 把诊断拆成多个小检查模块。
- 每个检查都有：
  - `id`
  - `category`
  - `status`
  - `message`
  - 可选 `fix`
- 最后统一输出 summary。

这套东西非常适合 agent / CLI / CI。

### 竞品 doctor 模块拆分

| 文件 | 作用 |
|---|---|
| `cli/src/doctor/mod.rs` | 诊断编排、summary、输出 |
| `cli/src/doctor/environment.rs` | HOME、state dir、socket dir、磁盘空间 |
| `cli/src/doctor/daemon.rs` | daemon inventory、版本匹配、stale cleanup 可见化 |
| `cli/src/doctor/chrome.rs` | 浏览器可执行、版本、缓存目录 |
| `cli/src/doctor/network.rs` | 网络可达性 |
| `cli/src/doctor/security.rs` | 安全和密钥类检查 |
| `cli/src/doctor/providers.rs` | provider 配置检查 |
| `cli/src/doctor/launch.rs` | 真实启动 smoke |

### 对 OpenPage 的建议

你不一定要照搬成 8 个模块，但至少建议把当前 `rust/src/cli/doctor.rs` 分成 3 层：

| 建议层次 | 说明 |
|---|---|
| Environment | `OPENPAGE_HOME`、daemon dir、可写性、配置源 |
| Daemon | inventory、incomplete sidecars、cleaned stale、ready/alive/version |
| Browser | `browser_path`、配置文件、可执行是否存在、可选真实启动测试 |

### 这部分是否能直接 copy

| 部分 | 结论 |
|---|---|
| `Check` / `Summary` 数据结构 | 可以直接借 |
| `run_doctor()` 的编排模式 | 可以直接借 |
| 具体 Chrome / provider 检查 | 只能参考，按 OpenPage 配置体系改 |

---

## 3.4 输出治理对 agent 很重要，且很容易借

竞品在 `cli/src/output.rs` 里做了两件很有价值的事：

1. **content boundary**：给正文包边界，降低 LLM 把页面正文和控制输出混淆的概率。
2. **truncate**：限制超长输出，避免 agent 被巨大 HTML / text 淹没。

这类设计对 OpenPage 非常合适，因为你本身就是 agent-friendly CLI。

### 竞品做法

| 能力 | 说明 |
|---|---|
| boundary nonce | 每次进程生成 nonce，降低伪造边界的概率 |
| origin 标记 | 标识内容来自哪个页面 / 输出来源 |
| max output | 对 text/html/value 做截断 |
| JSON 模式也带 `_boundary` 元数据 | 对 agent 更稳 |

### 对 OpenPage 的结论

- 这部分 **非常值得借**。
- 而且你项目里已经有对应形态：`rust/src/cli/protocol.rs`。
- 后续只需要继续收敛字段命名和覆盖面，不需要另起炉灶。

---

## 3.5 snapshot 值得借“契约”，不值得借“实现”

竞品 `snapshot` 的强点是：

- 返回的是 **agent-friendly contract**，不是原始 HTML。
- 有 `snapshot` 文本树。
- 有 `origin`。
- 有 `refs` 映射。

在 `cli/src/native/actions.rs:2466`，返回结构大致是：

- `snapshot`
- `origin`
- `refs`

### 这部分怎么借

| 层次 | 建议 |
|---|---|
| 输出契约 | 可以借 |
| ref 的表达方式 | 可以借 |
| AX tree 抽取算法 | 不建议直接抄 |
| iframe / active frame / ref_map 内部实现 | 不建议直接抄 |

原因很简单：

- 它的 `native/snapshot.rs` 和自己的 `cdp::types`、`element::RefMap`、`iframe_sessions` 深度绑定。
- 你如果硬抄，后面维护成本会很高。

---

## 3.6 install / upgrade 体验可以参考，但不是当前最高优先级

这块竞品做得比较完整：

| 模块 | 文件 | 值得学什么 |
|---|---|---|
| install | `cli/src/install.rs:400` | 自动下载 Chrome for Testing、Linux 依赖安装提示 |
| upgrade | `cli/src/upgrade.rs:46,187` | 自动识别安装来源并执行升级 |
| postinstall | `scripts/postinstall.js` | 写入 `.install-method` 标记，降低 upgrade 猜测成本 |

### 适合什么场景

- 如果你后面要把 OpenPage 做成 **对外分发 CLI**，这套很值得借。
- 如果你当前主要是本地研发和内用，优先级可以放后。

### 可直接借的点

| 点 | 结论 |
|---|---|
| `.install-method` marker 思路 | 很值得借 |
| `detect_install_method()` | 可直接参考 |
| 浏览器下载与缓存目录设计 | 可参考，但要按 OpenPage 的浏览器策略改 |
| Linux 依赖安装防踩坑逻辑 | 有价值，但不是当前必需 |

---

## 3.7 skills 体系是中期可借项

竞品把：

- `skills/` 作为 discovery stub
- `skill-data/` 作为运行时技能正文

这是一个比较成熟的 agent-facing 文档分层。

### 是否值得现在做

| 结论 | 原因 |
|---|---|
| 可借，但不急 | 这是产品化能力，不是 TCP daemon 主路径问题 |

如果你后续要让 OpenPage 更适合被 agent 调用，这套目录组织方式值得参考。

---

## 4. 哪些部分可以直接 copy 再微调

## 4.1 可以直接 copy 的高价值小块

| 竞品位置 | 建议级别 | 迁移说明 |
|---|---|---|
| `cli/src/connection.rs::walk_daemons()` | 很高 | 改成 TCP-only sidecar 扫描 |
| `cli/src/connection.rs::daemon_version_matches()` | 很高 | 基本可原样迁移 |
| `cli/src/connection.rs::send_command()` | 很高 | 改成 OpenPage `Request/Response` 类型 |
| `cli/src/connection.rs::is_transient_error()` | 很高 | 基本可原样迁移 |
| `cli/src/main.rs::run_close_all()` | 很高 | 适配你自己的 `browser stop` / `close` 语义 |
| `cli/src/main.rs::run_batch()` | 很高 | 适配 clap 命令树即可 |
| `cli/src/doctor/mod.rs` 的 `Check/Summary/run_doctor` | 很高 | 很适合直接借结构 |
| `cli/src/output.rs` 的 truncate / boundary 思路 | 很高 | 你已经有第一版，可继续细化 |
| `cli/src/upgrade.rs::detect_install_method()` | 中 | 对分发很有帮助 |

## 4.2 只能参考设计，别直接抄

| 竞品位置 | 原因 |
|---|---|
| `cli/src/native/actions.rs` | 大型命令分发器，和内部状态模型强耦合 |
| `cli/src/native/snapshot.rs` | 和 CDP AX tree / ref_map / iframe 机制强耦合 |
| `cli/src/native/browser.rs` | 浏览器生命周期和 provider 抽象是竞品内核 |
| `cli/src/native/element.rs` | 元素定位和 ref 解析全是竞品语义 |
| `cli/src/native/interaction.rs` | 点击、输入、拖拽等动作路径耦合很深 |
| `cli/src/native/network.rs` | 网络拦截模型和命令协议深绑 |

## 4.3 明确不建议抄

| 竞品部分 | 不建议原因 |
|---|---|
| 混合 Unix socket + Windows TCP 的连接抽象 | 你已经选定 TCP-only，再引回双分支只会增加复杂度 |
| 完整 snapshot 算法 | 维护成本高，移植后很容易半残 |
| chat / dashboard / stream 全家桶 | 如果当前目标只是稳定 CLI，这些会分散主线 |

---

## 5. 对 OpenPage 的具体落地映射

| 借鉴项 | 竞品文件 | OpenPage 目标文件 | 落地建议 |
|---|---|---|---|
| sidecar inventory | `cli/src/connection.rs` | `rust/src/cli/connection.rs` | 保留 `.port/.pid/.version`，删去 `.sock` 分支 |
| stale cleanup 可见化 | `cli/src/connection.rs` + `cli/src/doctor/daemon.rs` | `rust/src/cli/connection.rs` + `rust/src/cli/doctor.rs` | 在 doctor 中展示 incomplete / cleaned |
| close-all | `cli/src/main.rs:408` | `rust/src/cli/mod.rs` 或 `rust/src/cli/oneshot.rs` | 做一个统一关闭所有 session 的外层命令 |
| batch | `cli/src/main.rs:1305` | `rust/src/cli/oneshot.rs:708` | 保持 top-level orchestration，不碰浏览器内核 |
| doctor 框架 | `cli/src/doctor/mod.rs` | `rust/src/cli/doctor.rs` | 建议后续拆分子检查函数或子模块 |
| output boundary / truncate | `cli/src/output.rs` | `rust/src/cli/protocol.rs` | 已有基础，可继续增强更多字段覆盖 |
| snapshot contract | `cli/src/native/actions.rs:2466` | `rust/src/webpage.rs` / `rust/src/page.rs` / CLI 输出层 | 只统一 contract，不迁算法 |
| install / upgrade | `cli/src/install.rs` / `cli/src/upgrade.rs` / `scripts/postinstall.js` | 后续 packaging 层 | 作为产品化阶段再做 |

---

## 6. 如果后面只借一部分，我建议的顺序

### 第一批：直接带来稳定性
1. **daemon inventory + stale cleanup 可见化**
2. **version mismatch 自动重启**
3. **瞬时错误 retry**
4. **close-all**

### 第二批：直接带来 agent 体验
1. **batch**
2. **doctor**
3. **output boundary / truncate**

### 第三批：产品化增强
1. **install / upgrade**
2. **skills 发现与分层**
3. **stream / dashboard**

---

## 7. 当前最推荐的借鉴策略

### 推荐策略

| 策略 | 结论 |
|---|---|
| 直接 copy `connection.rs` 的“外壳治理逻辑” | 推荐 |
| 直接 copy `doctor` 的“检查框架” | 推荐 |
| 直接 copy `batch` 的“交互模型” | 推荐 |
| 直接 copy `output` 的“LLM 输出治理” | 推荐 |
| 直接 copy `native/*` 的浏览器动作内核 | 不推荐 |

### 原因

因为你当前的核心问题不是“怎么再造一套浏览器驱动”，而是：

- **TCP daemon 怎么稳定**
- **CLI 怎么更 agent-friendly**
- **诊断和恢复怎么更工程化**

而这些恰好就是 `agent-browser` 最值得抄的地方。

---

## 8. 本文涉及的主要文件

## 8.1 竞品文件

- `参考项目/agent-browser-main/cli/src/connection.rs`
- `参考项目/agent-browser-main/cli/src/main.rs`
- `参考项目/agent-browser-main/cli/src/commands.rs`
- `参考项目/agent-browser-main/cli/src/output.rs`
- `参考项目/agent-browser-main/cli/src/doctor/mod.rs`
- `参考项目/agent-browser-main/cli/src/doctor/environment.rs`
- `参考项目/agent-browser-main/cli/src/doctor/daemon.rs`
- `参考项目/agent-browser-main/cli/src/doctor/chrome.rs`
- `参考项目/agent-browser-main/cli/src/install.rs`
- `参考项目/agent-browser-main/cli/src/upgrade.rs`
- `参考项目/agent-browser-main/scripts/postinstall.js`
- `参考项目/agent-browser-main/cli/src/native/actions.rs`
- `参考项目/agent-browser-main/cli/src/native/snapshot.rs`
- `参考项目/agent-browser-main/cli/src/skills.rs`

## 8.2 OpenPage 对应文件

- `rust/src/cli/connection.rs`
- `rust/src/cli/mod.rs`
- `rust/src/cli/oneshot.rs`
- `rust/src/cli/doctor.rs`
- `rust/src/cli/protocol.rs`
- `rust/src/cli/args.rs`
- `rust/src/page.rs`
- `rust/src/webpage.rs`
- `rust/src/browser.rs`
- `README.md`
- `skills/`

---

## 9. 最后结论

对你当前这条 **TCP-only、全平台友好** 的路线来说，`agent-browser` 最该借的是：

1. **daemon 外壳治理**
2. **batch**
3. **doctor**
4. **输出治理**

最不该借的是：

1. **CDP / snapshot / element / interaction 内核**

所以后面的借鉴动作，建议严格按这个边界执行：

> **借协议外壳和工程治理；保留 OpenPage 自己的浏览器内核真相源。**
