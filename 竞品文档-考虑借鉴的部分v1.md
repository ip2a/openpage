# 竞品文档：考虑借鉴的部分 v1

## 0. 结论先行

在你已经确定 **OpenPage 统一走 TCP daemon** 的前提下，`agent-browser` 最值得学习的不是浏览器/CDP 内核，而是它的 **CLI 外壳层**。

一句话判断：

> **可以借“daemon / doctor / output / agent-docs”这四层；不要借 `cli/src/native/*` 这一层。**

对你现在最有价值的点，按优先级排：

1. `connection.rs` 的 daemon sidecar / inventory / stale cleanup / version mismatch restart
2. `doctor/*.rs` 的结构化诊断模型：`Check + Summary + fix hint + launch guard`
3. `output.rs` 的 boundary / truncate / trust-boundary 思路
4. `skill-data/core/*` 的 agent 文档组织方式

不建议现在碰的：

- `cli/src/native/*`
- `cli/src/chat.rs`
- `packages/dashboard/*`
- providers / cloud 扩展

---

## 1. 本次分析边界

### 1.1 你的既定边界

| 边界 | 结论 |
|---|---|
| 通信模式 | **TCP-only**，这是正确方向 |
| 浏览器/CDP/元素交互内核 | **继续保留 OpenPage 自己的实现** |
| 竞品借鉴范围 | **只借外壳层，不借内核层** |

### 1.2 本次主要参考文件

> 定位方式：基于现有本地检索结果 + `rg`/结构扫描；重点看“可借鉴外壳层”，不追竞品底层 CDP 细节。

#### 竞品文件

| 文件 | 用途 |
|---|---|
| `参考项目/agent-browser-main/cli/src/connection.rs` | daemon 连接、sidecar、inventory、自动拉起、重试 |
| `参考项目/agent-browser-main/cli/src/doctor/mod.rs` | doctor 主框架、summary、text/json 输出 |
| `参考项目/agent-browser-main/cli/src/doctor/launch.rs` | 浏览器 launch smoke、guard 清理 |
| `参考项目/agent-browser-main/cli/src/output.rs` | 输出治理、boundary、truncate |
| `参考项目/agent-browser-main/cli/src/main.rs` | CLI 主调度、batch 主循环、统一错误输出 |
| `参考项目/agent-browser-main/cli/src/commands.rs` | 命令解析与批处理输入模式 |
| `参考项目/agent-browser-main/skill-data/core/SKILL.md` | agent 入口技能说明 |
| `参考项目/agent-browser-main/skill-data/core/references/snapshot-refs.md` | ref 驱动交互文档 |
| `参考项目/agent-browser-main/skill-data/core/references/session-management.md` | session 文档 |
| `参考项目/agent-browser-main/skill-data/core/references/trust-boundaries.md` | trust boundary 文档 |

#### OpenPage 对应承接文件

| 文件 | 当前职责 |
|---|---|
| `rust/src/cli/connection.rs` | TCP daemon sidecar、inventory、auto-start、重试 |
| `rust/src/cli/doctor.rs` | doctor 检查、fix hint、launch smoke |
| `rust/src/cli/protocol.rs` | NDJSON request/response、boundary、truncate |
| `rust/src/cli/serve.rs` | TCP daemon 服务端、snapshot contract、session runtime state |
| `rust/src/cli/oneshot.rs` | CLI wrapper、batch、统一 RPC 出口 |
| `rust/src/cli/args.rs` | CLI 命令面定义 |
| `skills/openpage-test/*` | agent-facing 使用文档 |

---

## 2. 当前判断：OpenPage 已经借到哪一步

| 能力 | 当前状态 | 判断 |
|---|---|---|
| TCP daemon 主路径 | 已收敛 | 方向正确，不要回退 |
| sidecar / inventory | 已接第一版 | 可以继续向竞品结构收敛 |
| doctor / quick doctor | 已接第一版 | 最值得继续补强 |
| boundary / truncate | 已接 | 保持即可，继续润色输出细节 |
| batch | 已接 | 够用，不是当前主战场 |
| snapshot text/refs/origin/title/interactive_count | 已接第一版 | 契约方向正确，底层不要替换 |
| trust-boundary / session / snapshot-refs 文档 | 已接第一版 | 适合继续补齐 |

### 2026-05-30 本地核验事实

| 核验项 | 结果 |
|---|---|
| `cargo check --manifest-path rust/Cargo.toml` | 通过 |
| `openpage browser list` | 最新实测 4 个 healthy sessions，`incomplete=[]`，`cleaned=[]` |
| `openpage doctor --quick` | 当前唯一 fail 是 `browser.executable` |
| 本机可用浏览器路径 hint | `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome` |

这说明一件事：

> **OpenPage 当前已经不需要重做协议层，而是继续补强 TCP daemon 的外围工程质量。**

---

## 3. 借鉴优先级总表

| 优先级 | 借鉴项 | 竞品文件 | 是否适合直接 copy/微调 | OpenPage 落点 | 说明 |
|---|---|---|---|---|---|
| P0 | daemon sidecar / inventory / stale cleanup | `cli/src/connection.rs` | **适合** | `rust/src/cli/connection.rs` | 和你当前 TCP-only 方向完全一致 |
| P0 | version mismatch restart | `cli/src/connection.rs` | **适合** | `rust/src/cli/connection.rs` | 能避免旧 daemon 与新 CLI 协议错配 |
| P0 | doctor `Check + Summary + fix` 结构 | `cli/src/doctor/mod.rs` | **适合** | `rust/src/cli/doctor.rs` | 高性价比，直接提升可诊断性 |
| P0 | launch cleanup guard | `cli/src/doctor/launch.rs` | **适合** | `rust/src/cli/doctor.rs` | 你已经部分借了，值得继续收敛 |
| P0 | output boundary / truncate | `cli/src/output.rs` | **适合** | `rust/src/cli/protocol.rs` | 已有基础，实现层很容易继续靠拢 |
| P1 | batch 主循环 | `cli/src/main.rs` | **部分适合** | `rust/src/cli/oneshot.rs` | 借执行骨架，不要搬命令体系 |
| P1 | stdin JSON batch 约定 | `cli/src/main.rs`, `cli/src/commands.rs` | **适合** | `rust/src/cli/oneshot.rs` | agent 友好，已经有价值 |
| P1 | snapshot-refs 文档结构 | `skill-data/core/references/snapshot-refs.md` | **适合** | `skills/openpage-test/*` | 借文档结构，不借底层 snapshot 算法 |
| P1 | session-management 文档结构 | `skill-data/core/references/session-management.md` | **适合** | `skills/openpage-test/*` | 很适合 OpenPage 的 named session 心智 |
| P1 | trust-boundaries 文档结构 | `skill-data/core/references/trust-boundaries.md` | **适合** | `skills/openpage-test/*` | 对 agent 非常重要 |
| P2 | CLI 错误输出包装 | `cli/src/main.rs` | **可借** | `rust/src/cli/oneshot.rs` / 顶层 CLI 入口 | 主要是统一 JSON error 形态 |
| P3 | chat REPL | `cli/src/chat.rs` | **不建议现在做** | 无 | 会把主线带偏 |
| P3 | dashboard | `packages/dashboard/*` | **不建议现在做** | 无 | 对 TCP daemon 主路径帮助不大 |
| P3 | 浏览器/provider 云扩展 | `cli/src/native/providers.rs` 等 | **不建议现在做** | 无 | 你现在不是做多 provider 平台 |

---

## 4. 最值得“直接 copy 再微调”的函数/结构清单

这部分是最实用的。

### 最新补充（2026-05-30，本地已落地的借鉴点）

| 本轮落地点 | 状态 | 说明 | 涉及文件 |
|---|---|---|---|
| 同 session stale daemon 处理 | **已补强** | `ensure_daemon()` 现在会先判断“复用 / 杀掉 / 忽略”，对 **旧进程仍活着但 TCP 不可用** 的情况增加 ready 宽限期与 kill-before-respawn，避免同 session 留下孤儿进程或并存 daemon | `rust/src/cli/connection.rs` |
| 旧 session JSON 残留删除 | **已删除** | `browser stop` 不再碰 `sessions/<name>.json` 这类旧时代残留，活跃 CLI 面进一步收口到 TCP sidecar + daemon shutdown | `rust/src/cli/oneshot.rs` |

### 4.1 `connection.rs`：最适合直接借的一层

| 竞品结构/函数 | 竞品文件 | 借鉴价值 | OpenPage 对应位置 | 建议怎么借 |
|---|---|---|---|---|
| `ActiveSession` | `cli/src/connection.rs` | sidecar 扫描结果模型清晰 | `rust/src/cli/connection.rs` | 可基本照抄结构，再按 OpenPage 字段精简 |
| `CleanReason` | `cli/src/connection.rs` | stale/incomplete 分类很有用 | `rust/src/cli/connection.rs` | 可直接借分类思路 |
| `CleanedSession` | `cli/src/connection.rs` | 让 cleanup 结果可观测 | `rust/src/cli/connection.rs` | 适合保留 |
| `DaemonInventory` | `cli/src/connection.rs` | `healthy/incomplete/cleaned` 很实用 | `rust/src/cli/connection.rs` + `doctor.rs` | 这是最值得保留的产物模型 |
| `cleanup_stale_files()` | `cli/src/connection.rs` | 生命周期管理成熟 | `rust/src/cli/connection.rs` | 可继续按 TCP-only 规则微调 |
| `walk_daemons()` | `cli/src/connection.rs` | 扫描/分类/清理主逻辑清晰 | `rust/src/cli/connection.rs` | 很适合继续对齐 |
| `daemon_version_matches()` | `cli/src/connection.rs` | 版本错配重启很关键 | `rust/src/cli/connection.rs` | 已有就继续保持 |
| `kill_stale_daemon()` | `cli/src/connection.rs` | 旧 daemon 清理策略成熟 | `rust/src/cli/connection.rs` | 直接借思路即可 |
| `DaemonOptions` | `cli/src/connection.rs` | 自动拉起参数归拢清楚 | `rust/src/cli/connection.rs` | 适合保留“配置集中传递”的做法 |
| `ensure_daemon()` | `cli/src/connection.rs` | 整个 daemon 自动拉起骨架最值钱 | `rust/src/cli/connection.rs` | **重点借**，但删掉 Unix socket 分支，只保留 TCP |
| `is_transient_error()` | `cli/src/connection.rs` | 重试逻辑简单有效 | `rust/src/cli/connection.rs` | 可以直接借判定思路 |
| `send_command()` / `send_command_once()` | `cli/src/connection.rs` | 请求-重试-错误归一化骨架成熟 | `rust/src/cli/connection.rs` | 只借流程，不借它的连接实现细节 |

### 4.2 `doctor`：当前最该继续抄的一层

| 竞品结构/函数 | 竞品文件 | 借鉴价值 | OpenPage 对应位置 | 建议怎么借 |
|---|---|---|---|---|
| `DoctorOptions` | `cli/src/doctor/mod.rs` | 主入口参数清晰 | `rust/src/cli/doctor.rs` | 可参考组织方式 |
| `Check` | `cli/src/doctor/mod.rs` | 诊断项结构化输出核心 | `rust/src/cli/doctor.rs` | **继续对齐字段** |
| legacy artifact audit | `cli/src/doctor/*`（思路层） | 让已废弃执行路径留下的本地残留物可见 | `rust/src/cli/doctor.rs` | **已接入一部分**：现在会检查 `OPENPAGE_HOME/sessions/*.json` 并给 fix 建议 |
| `Summary` | `cli/src/doctor/mod.rs` | 汇总统计清晰 | `rust/src/cli/doctor.rs` | 适合保持一致 |
| structured machine summary | `cli/src/doctor/mod.rs`（方向相同） | 让 agent 不必重扫全部 checks 就能知道 info/fixable/total | `rust/src/cli/doctor.rs` | **已继续补强**：summary 现在包含 `info` / `fixable` / `total` |
| `summarize()` | `cli/src/doctor/mod.rs` | 把检查项汇总成统一结果 | `rust/src/cli/doctor.rs` | 很适合继续沿用 |
| `print_text()` | `cli/src/doctor/mod.rs` | 人读输出很成熟 | `rust/src/cli/doctor.rs` | 可借文案布局 |
| `print_json()` | `cli/src/doctor/mod.rs` | 机器消费输出清晰 | `rust/src/cli/doctor.rs` | 可继续对齐 JSON 形态 |
| `LaunchGuard` | `cli/src/doctor/launch.rs` | 浏览器 launch smoke 清理很稳 | `rust/src/cli/doctor.rs` | **重点继续借** |
| launch smoke 生命周期 | `cli/src/doctor/launch.rs` | 防止临时目录/浏览器残留 | `rust/src/cli/doctor.rs` | 你已有 `TempDirGuard` / `BrowserLaunchGuard`，继续收紧即可 |

### 4.3 `output.rs`：适合小步照抄的层

| 竞品结构/函数 | 竞品文件 | 借鉴价值 | OpenPage 对应位置 | 建议怎么借 |
|---|---|---|---|---|
| `get_boundary_nonce()` | `cli/src/output.rs` | boundary 防伪核心 | `rust/src/cli/protocol.rs` | 你已接 CSPRNG，不要回退 |
| `OutputOptions` | `cli/src/output.rs` | 输出治理参数集中 | `rust/src/cli/protocol.rs` | 适合继续保持集中配置 |
| `truncate_if_needed()` | `cli/src/output.rs` | 文本截断逻辑简单稳定 | `rust/src/cli/protocol.rs` | 可以继续细修文案 |
| `print_with_boundaries()` | `cli/src/output.rs` | 面向 agent 的可信边界输出 | `rust/src/cli/protocol.rs` | 借结构，不必逐字照搬 |
| origin-aware boundary metadata | `cli/src/output.rs` | 让 agent 知道这段页面内容来自哪个 origin，更接近 trust-boundary 设计 | `rust/src/cli/protocol.rs` | **已接入**：`_boundary.origin` + wrapped marker `origin=...` |
| `print_response_with_opts()` | `cli/src/output.rs` | action-response 统一输出出口 | OpenPage 可映射到 CLI 顶层 JSON 输出链路 | 适合借“统一出口”思想 |

### 4.4 文档层：代码量不大，但收益很高

| 竞品文档 | 借鉴价值 | OpenPage 落点 | 建议 |
|---|---|---|---|
| `skill-data/core/SKILL.md` | 让 agent 知道什么时候该用什么能力 | `skills/openpage-test/SKILL.md` | 借路由结构和任务分类 |
| `references/snapshot-refs.md` | 明确 ref 交互心智 | `skills/openpage-test/references/snapshot-refs.md` | 非常值得继续补细节 |
| `references/session-management.md` | 明确多 session 流程 | `skills/openpage-test/references/session-management.md` | 继续补 named session 最佳实践 |
| `references/trust-boundaries.md` | 抗 prompt injection、边界说明 | `skills/openpage-test/references/trust-boundaries.md` | 对 agent 非常关键 |

---

## 5. 哪些部分只借“设计”，不要直接 copy

| 模块 | 竞品文件 | 原因 |
|---|---|---|
| 浏览器启动/控制内核 | `cli/src/native/browser.rs` | 强绑定竞品自己的浏览器抽象 |
| action dispatch | `cli/src/native/actions.rs` | 强绑定竞品 action schema |
| daemon 内核 | `cli/src/native/daemon.rs` | 和它自己的 native runtime 强耦合 |
| element / interaction | `cli/src/native/element.rs`, `cli/src/native/interaction.rs` | 和它的 ref / DOM / action 模型绑太死 |
| snapshot 底层算法 | `cli/src/native/snapshot.rs` | 可借 contract，不要借算法 |
| network / provider 扩展 | `cli/src/native/network.rs`, `cli/src/native/providers.rs` | 当前不是你的主线 |

一句话边界：

> **借“怎么包 CLI/daemon”，不要借“怎么驱浏览器和 DOM”。**

---

## 6. 对 OpenPage 的具体落点建议

### 6.1 已经在承接竞品外壳设计的文件

| OpenPage 文件 | 建议定位 |
|---|---|
| `rust/src/cli/connection.rs` | 继续作为 daemon 发现、重试、inventory、version handling 的唯一入口 |
| `rust/src/cli/doctor.rs` | 继续收敛成结构化诊断中心，不要分散到别处 |
| `rust/src/cli/protocol.rs` | 继续作为 boundary / truncate / JSON output 的唯一治理点 |
| `rust/src/cli/serve.rs` | 继续只负责 TCP daemon 和 RPC contract，不要塞入竞品浏览器内核逻辑 |
| `rust/src/cli/oneshot.rs` | 继续作为命令壳层和 batch/统一 RPC 包装层 |
| `skills/openpage-test/*` | 继续承接 agent-facing 心智文档 |

### 6.2 当前不该为了“借鉴”而动的文件

| OpenPage 文件方向 | 原因 |
|---|---|
| `rust/src/browser.rs` | 这是你自己的浏览器真相源 |
| `rust/src/page.rs` | 不该被竞品外壳设计污染 |
| 元素定位 / 交互细节实现 | 这是你的产品差异，不该为了省事替换 |
| Python API 表层兼容逻辑 | 当前任务核心不是这个 |

---

## 7. 如果现在继续推进，建议顺序

| 顺序 | 动作 | 原因 |
|---|---|---|
| 1 | 继续补 `rust/src/cli/doctor.rs` 的 `Check / Summary / fix / guard` 一致性 | 投入小，收益最大 |
| 2 | 继续补 `skills/openpage-test/references/*` 三份文档 | 直接提高后续 agent 成功率 |
| 3 | 继续统一 CLI 顶层错误/JSON 输出 | 能减少后续脚本和 agent 接口噪音 |
| 4 | 小步打磨 `connection.rs` 的 inventory / retry / stale cleanup 细节 | 巩固 TCP-only 稳定性 |
| 5 | 暂停 chat/dashboard/provider 扩展 | 当前性价比低 |

---

## 8. 真要“直接抄”，优先抄哪些文件

### 第一优先级

- `参考项目/agent-browser-main/cli/src/connection.rs`
- `参考项目/agent-browser-main/cli/src/doctor/mod.rs`
- `参考项目/agent-browser-main/cli/src/doctor/launch.rs`
- `参考项目/agent-browser-main/cli/src/output.rs`

### 第二优先级

- `参考项目/agent-browser-main/cli/src/main.rs`
- `参考项目/agent-browser-main/cli/src/commands.rs`
- `参考项目/agent-browser-main/skill-data/core/SKILL.md`
- `参考项目/agent-browser-main/skill-data/core/references/snapshot-refs.md`
- `参考项目/agent-browser-main/skill-data/core/references/session-management.md`
- `参考项目/agent-browser-main/skill-data/core/references/trust-boundaries.md`

### 明确不要碰

- `参考项目/agent-browser-main/cli/src/native/*`
- `参考项目/agent-browser-main/cli/src/chat.rs`
- `参考项目/agent-browser-main/packages/dashboard/*`

---

## 9. 许可证提醒

竞品当前仓库许可证是 **Apache-2.0**。

如果只是借思路，问题不大。
如果后面准备复制成块代码，至少确认两件事：

1. OpenPage 当前许可证是否兼容
2. 是否需要保留原始 copyright / notice

---

## 10. 最后的工程判断

你现在不缺“再换一种浏览器控制内核”。
你真正缺的，是把现有 OpenPage 内核继续包装成一个：

- **TCP-only**
- **跨平台稳定**
- **可诊断**
- **适合 agent 消费**
- **输出边界清晰**
- **文档心智统一**

的 CLI/daemon 体系。

`agent-browser` 对你最有用的价值，正好就在这里。
